# PM-T004 流程设置像素蓝图

状态：`draft / pending visual confirmation`。本文件是实现前的测量基线，不授权开始前端编码。

`PM-T004-template-flow-settings.png` 是前一版近似草图，不能作为像素验收证据；在阻塞项确认后必须由实现页面重新生成独立截图。

## 1. 来源与坐标约定

- 来源文件：`C:\Users\gaby.liu\Downloads\page-capture\capture.json`、`initial.html`、`screenshots/state-1.png` 至 `state-47.png`。
- 基准状态：`state-1`（初始、未选中节点）。
- 验收参数基准：采用 `initial.html` 的 `meta viewport=1300`；此前 capture 的 `1280 × 639 CSS px` 仅作录制几何参考，不得覆盖本次 SnapSpec 组件参数。
- capture 录制仍为 `devicePixelRatio=1.5`、截图物理尺寸 `1920 × 959 device px`；所有实现尺寸以组件参数的 CSS px 为准。
- 页面滚动：基准状态 `scrollX=0, scrollY=0`；根应用固定覆盖 `1280 × 639.333 CSS px`，header 固定在顶部。
- `initial.html` 的 `meta viewport=1300` 是本任务的验收基准；capture viewport `1280` 只用于定位录制状态和相对结构。

## 2. 页面分区（基准状态）

| 区域 | CSS 矩形 | device 矩形（四舍五入） | 规则 |
|---|---|---|---|
| Header | `(0,0)-(1280,56)` | `(0,0)-(1920,84)` | `position:absolute/fixed`，`padding:0 20px 0 16px` |
| 主体 | `(0,56)-(1280,639.333)` | `(0,84)-(1920,959)` | `display:flex`，`overflow:auto` |
| 画布 | `(0,56)-(960,639.333)` | `(0,84)-(1440,959)` | 独立滚动容器，纵向流程居中 |
| 右面板 | `(960,56)-(1280,639.333)` | `(1440,84)-(1920,959)` | 宽 `320`，左边界为画布右边界 |
| “评估流程模板”入口 | `(20,76)-(145.333,108)` | `(30,114)-(218,162)` | `margin-top:20; margin-left:20; padding:4px 11px; border:0.666667px solid rgb(208,211,214)` |

## 3. Header 共享组件

Header 必须实现为创建/编辑绩效模板向导的单一共享组件，不能在“基本信息 / 流程设置 / 内容设置 / 模板预览”页面分别复制实现。各步骤只传入当前 `step` 和导航能力，组件结构、分隔线、步骤间箭头、左右按钮布局保持一致。

### 3.1 固定结构

| 元素 | CSS 矩形 | 颜色/边框 |
|---|---|---|
| 返回组合容器 | `(16,13.667)-(225,41.667)` | 文字 `rgb(31,35,41)` |
| 返回文字 | `(48,15.667)-(80,39.667)` | 捕获文字框 `32×24` |
| 分隔线 | x=`144`（具体宽度未在元素矩形中单独暴露） | 颜色未确认 |
| 标题“编辑绩效模板” | `(113,15.667)-(209,39.667)` | `rgb(31,35,41)` |
| 步骤容器 | `(225,11.667)-(1260,43.667)` | `display:flex` |
| 基本信息 | `(459,15.667)-(515,39.667)` | `rgb(31,35,41)` |
| 流程设置 | `(561,15.667)-(617,39.667)` | active `rgb(51,112,255)` |
| 内容设置 | `(663,15.667)-(719,39.667)` | disabled `rgb(143,149,158)` |
| 模板预览 | `(765,15.667)-(821,39.667)` | disabled `rgb(143,149,158)` |
| 按钮组 | `(1088,11.667)-(1260,43.667)` | `gap:12` |
| 上一步 | `(1088,11.667)-(1168,43.667)` | `80×32`，`padding:4px 11px`，`border:0.666667px solid rgb(208,211,214)` |
| 下一步 | `(1180,11.667)-(1260,43.667)` | `80×32`，`padding:4px 11px`，`border:0.666667px solid rgb(51,112,255)` |

返回图标、标题、步骤项、步骤箭头、上一步/下一步按钮和 Header 分隔线属于共享组件的固定结构。流程设置页不得改变 Header 高度、内边距、步骤间距或按钮位置。

### 3.2 步骤状态颜色

| 状态 | 文案颜色 | 行为 |
|---|---|---|
| 当前步骤 | `#3370FF` / `rgb(51,112,255)` | 当前步骤高亮；流程设置页为当前步骤 |
| 已完成且可返回步骤 | `#1F2329` / `rgb(31,35,41)` | 可点击，点击触发保存/返回逻辑 |
| 未到达步骤 | `#8F959E` / `rgb(143,149,158)` | 禁止直接进入 |

Header 分隔线、步骤箭头、返回图标的具体 SVG path 来自既有 Header 组件；若现有基本信息实现已有快照或组件源码，应直接作为唯一实现来源，不在本任务中重新绘制。

## 4. 画布与流程节点

画布中心线 `x=480 CSS px`（device x=`720`）。基准状态节点顺序固定为：项目启动 → 评估型环节 → 绩效结果查看环节 → 项目结束。系统节点 y 间距由捕获值计算，不能改成按比例布局。

| 节点 | CSS 矩形 | 内部布局/边框 |
|---|---|---|
| 项目启动 | `(430,96)-(530,136)` | `100×40`，`display:flex`，`border:0.666667px solid rgba(187,191,196,0.5)` |
| 评估型环节 | `(414.333,176.667)-(545.667,242.667)` | `131.333×66`，`padding:12px 16px`，普通边框 `0.666667px solid rgb(222,224,227)` |
| 绩效结果查看环节 | `(393.333,283.333)-(566.667,349.333)` | `173.333×66`，`padding:12px 16px`，普通边框同上 |
| 项目结束 | `(430,390)-(530,430)` | `100×40`，系统节点边框同项目启动 |

业务节点宽度由捕获内容决定，已确认：工作总结环节 `145.333×66`、360°邀请环节 `145.260×66`、校准环节 `177.333×66`、结果沟通环节 `129.333×66`。节点标题和“执行人：…”均为节点内部文本；图标实际 path/尺寸需按附件 DOM/SVG 复核，不能自行替换图标。

最新 StageBox SnapSpec 与 2026-08-14 用户确认对实现规则作如下覆盖：画布业务节点不得按节点类型写死宽度，使用内容宽度与 `min-width:0; max-width:100%`；流程容器同样不固定为捕获结果 `202px`，而应随最长节点内容增长，并保留左右各 `40px` padding。`202×441px` 和 `122×66px` 是该次捕获内容产生的测量结果，不是固定 CSS 宽度。节点在父级可用宽度内随名称自适应增长，达到 `max-width:100%` 上限后，标题和执行人文本使用 `overflow:hidden; text-overflow:ellipsis; white-space:nowrap` 显示省略号。节点内部固定为两行：第一行是 `display:flex; justify-content:center; align-items:center` 的图标与标题组合，图标 `20×20px`、右间距 `8px`、标题 `14px/21px`；第二行是独立的执行人文本，`12px/20px`、居中。

交互补充（2026-08-14 用户确认）：指针进入连接器加号或键盘焦点进入加号时打开“可添加环节”弹层，点击加号仍保持兼容；指针从加号移向弹层内容期间弹层保持可交互，离开触发区和弹层后关闭。可删除业务节点无需先点击选中，指针悬停节点或键盘焦点进入节点时即显示右侧删除按钮；系统节点和内置“绩效结果查看环节”不显示删除按钮。删除图标使用用户提供的 `DeleteTrashOutlined` 原始 path，SVG `16×16px`、颜色 `#646A73`。流程必须始终至少保留 1 个“评估型环节”：当评估型环节数量为 1 时不显示其删除按钮，删除方法也必须拒绝删除；数量大于 1 时才允许删除评估型环节，删除至剩余 1 个后立即重新隐藏删除按钮。其他非系统业务节点不受该最小数量规则影响。

节点 hover 状态补充（2026-08-14 SnapSpec）：鼠标进入业务节点时必须与点击选中态使用同一高亮值，边框和标题/图标颜色 `#3370FF`、背景 `rgba(51,112,255,0.08)`，执行人文字仍为 `#646A73`。删除按钮为 `24×25px`、padding `4px`、圆角 `6px`；默认背景透明，按钮 hover/focus 背景为 `rgba(31,35,41,0.2)`，使用附件定义的颜色/背景/边框/宽度 transition。现有节点右边界到按钮可见区域为 `4px`，该间距必须纳入连续 hover 命中区，指针从节点移动到删除按钮时不得导致按钮消失。

删除按钮显示规则（2026-08-14 最新用户确认，覆盖此前选中态补充）：删除按钮唯一的显示条件是鼠标指针位于该可删除业务节点的连续 hover 命中区内。节点被点击选中、通过新增自动选中或获得键盘焦点时，均不得让删除按钮在鼠标移出后持续显示。系统节点、内置“绩效结果查看环节”和最后 1 个评估型环节继续执行既有删除保护。

连接线、向下箭头和加号按钮的确切矩形未在本版写入，原因是 capture 将其拆成多个无文本 SVG/伪元素，当前尚未取得稳定 selector；这不是可猜测项。

## 5. 右侧面板基准与控件

- 选中节点时，右侧面板顶部公共标题栏为 `319×48px`：`padding:12px 20px`、系统字体 `16px`、字重 `600`、行高 `24px`、颜色 `rgba(0,0,0,.65)`，底部边框 `0.666667px solid rgba(31,35,41,.15)`。标题显示节点类型标准名称，不随“环节名称”输入框中的自定义名称变化。
- 标题栏与 `.p-12` 表单区域为同级元素；外层右侧面板 `overflow:hidden`，`.p-12` 单独 `overflow:auto`。滚动表单时标题栏保持原位置，不依赖 `position:sticky`。

- 未选中空态：面板 `(960,56)-(1280,639.333)`；空态内容矩形 `(960,376)-(1279.333,639.333)`，`margin-top:320px`，文字颜色 `rgb(143,149,158)`。
- 选中面板内容起始 x=`984`（左 padding `24px`）。
- 原生 input 内容区常见 x=`995.667`，宽 `243` 或 `251`（取决于滚动条），高 `22`。
- textarea 矩形为 `263.333×97.333` 或 `271.333×97.333`，`padding:4px 11px`，边框 `0.666667px solid rgb(208,211,214)`。
- checkbox 与 radio 均为 `16×16`；checkbox 项垂直间距约 `30px` 的“约”仅表示录制观察，正式实现必须以对应状态元素 rect 复测。
- switch 外框 `28×16`，handler `12×12`。

已从 state-2 确认的评估型字段文案：环节名称、环节描述、执行人（实线上级、直属上级、隔 1 级上级、隔 2 级上级、隔 3 级上级及以上）、评估类型（单人评估、多人评估）、“设置此环节绩效结果为最终结果”。其余节点字段以 state-23 至 state-40 的实际 DOM 为准，未提取的字段不得补写。

## 6. 已确认 SnapSpec 组件参数

### 6.1 已有周期提示条（`Div.flex`）

- 参数来源：用户提供的 SnapSpec，组件尺寸 `1200×40px`；`meta viewport=1300` 下按 CSS px 实现。
- 布局：`display:flex; flex-direction:row; align-items:center; height:40px; padding:0 16px; box-sizing:border-box`。
- 字体：系统字体栈 `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif`；`font-size:14px; font-weight:400; line-height:22px`。
- 背景：`#E1EAFF`；正文 `#1F2329`；链接 `#1890FF`；信息图标 `#3370FF`；关闭图标 `#646A73`。
- 信息图标与关闭图标均为 `16×16px` SVG；信息图标右间距 `8px`，关闭图标左间距 `8px`。
- 文案：链接文本为 `1 个周期的项目`；数量必须由接口返回，不能写死 `1`。
- 内容容器：`flex:1; min-width:0; overflow:hidden; white-space:nowrap`。
- 关闭按钮：`cursor:pointer`，必须保留关闭交互；hover/active 动效未在 SnapSpec 中给出，暂不添加未确认的视觉值。

### 6.2 `StageCard` 组件

- 参数来源：用户提供的 SnapSpec `Div.style__StageCard-sc-13nih8v-7`；所有可添加业务节点尺寸统一为 `280×38px`，差异只来自文案和前置 `16×16px` SVG 图标。
- 布局：`display:flex; align-items:center; min-width:0; min-height:0; padding:8px 12px; box-sizing:border-box`。
- 间距：每个弹层节点包含 `margin-bottom:8px`；画布节点不套用此弹层尺寸。
- 字体：同上系统字体栈；`font-size:14px; font-weight:400; line-height:22px; text-align:left`。
- 颜色：文字 `#1F2329`，背景 `#F5F6F7`，边框 `#DEE0E3`，边框宽度 `0.666667px`，`border-radius:4px`，`cursor:pointer`。
- 图标容器：`margin-right:8px`，图标 SVG `16×16px`，颜色 `#646A73`。
- 已确认的可新增 `StageCard` 节点与图标映射：`360°邀请环节`、`360°确认环节`、`评估型环节`、`校准环节`、`结果沟通环节`、`工作总结环节` 均使用用户提供的对应 `16×16px` SVG path。`绩效结果查看环节` 不在加号弹层中，不得渲染为 `StageCard`，也不得出现在可新增列表。
- 新增数量规则（2026-08-14 用户确认）：`评估型环节`可重复添加且始终保留在“可添加环节”列表中；`工作总结环节`、`360°邀请环节`、`360°确认环节`、`校准环节`、`结果沟通环节`各自最多存在 1 个，流程中已有对应类型时必须从列表隐藏，删除后重新显示。添加方法本身也必须拒绝绕过 UI 的重复添加。
- 特殊位置节点（2026-08-14 用户确认）：新增“结果复议处理”，稳定类型为 `result_reconsideration`。只有紧接“绩效结果查看环节”的连接器加号可展示该类型，并且该弹层只能展示这一项；其他连接器不得展示或插入。添加后结果查看节点及结果复议处理节点后的连接器仅保留 `16×41px` 流程连线/箭头，不显示可操作加号；删除该节点后恢复结果查看节点后的加号。该类型最多存在 1 个。
- “结果复议处理”弹层图标与画布图标共用下方原始 SVG；弹层按统一规则渲染为 `16×16px`，画布按统一规则渲染为 `20×20px`，颜色 `#646A73`。

```html
<svg viewBox="0 0 24 24" class="fill-current text-N-600 w-8 h-8">
  <path fill-rule="evenodd" clip-rule="evenodd" d="M16.275 9.664l-6.003-4.203-3.401 4.857 6.003 4.203 3.401-4.857zM9.88 3.232l-5.236 7.479 8.624 6.039 5.237-7.479L9.88 3.232z"></path>
  <path fill-rule="evenodd" clip-rule="evenodd" d="M7.901 3.026l1.036.725a.4.4 0 01.098.557L5.363 9.55a.4.4 0 01-.557.098l-1.035-.725a.4.4 0 01-.098-.557l3.671-5.243a.4.4 0 01.557-.098zm1.953-.586a2 2 0 01.492 2.786l-3.672 5.243a2 2 0 01-2.785.491l-1.036-.725a2 2 0 01-.491-2.786l3.671-5.243a2 2 0 012.786-.491l1.035.725zm5.385 14.514l-1.036-.725a.4.4 0 01-.098-.557l3.671-5.243a.4.4 0 01.558-.098l1.035.725a.4.4 0 01.099.557l-3.672 5.243a.4.4 0 01-.557.098zm-1.954.586a2 2 0 01-.49-2.786l3.67-5.243a2 2 0 012.786-.491l1.036.725a2 2 0 01.491 2.786l-3.671 5.243a2 2 0 01-2.786.491l-1.035-.725zm-7.343 2.152l-1.427-1a.4.4 0 01-.099-.557l3.561-5.086 2.083 1.459-3.56 5.085a.4.4 0 01-.558.099zm-2.345.31a2 2 0 01-.491-2.785l4.478-6.396 4.705 3.294-4.479 6.396a2 2 0 01-2.785.491l-1.428-1z"></path>
  <path d="M13.576 20.823a.5.5 0 01.5-.5h7a.5.5 0 01.5.5v.6a.5.5 0 01-.5.5h-7a.5.5 0 01-.5-.5v-.6z"></path>
</svg>
```
- 画布中的 `绩效结果查看环节` 使用本次补充的专属 `20×20px` SVG（viewBox `0 0 24 24`，颜色 `#646A73`，系统字体 `14px/21px`）。实现必须按映射使用原 path，不得用通用占位图标。

绩效结果查看环节 SVG 原始 path：

```html
<svg width="20" height="20" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" class="fill-current text-N-600 w-10 h-10">
  <path d="M7.8 3H5.498A1.999 1.999 0 003.5 5v15a2 2 0 002 2h6.6a.5.5 0 00.5-.5v-.6a.5.5 0 00-.5-.5H5.6a.5.5 0 01-.5-.5V5.1a.5.5 0 01.499-.5h2.2V3zm8.4 1.6h2.201c.276 0 .499.22.499.497v6.882c0 .276.224.498.5.498h.6c.276 0 .5-.226.5-.502V5.001A2 2 0 0018.502 3H16.2v1.6z"></path>
  <path fill-rule="evenodd" clip-rule="evenodd" d="M16.449 21.313c.95 0 1.828-.31 2.538-.835l1.425 1.363a.5.5 0 00.707-.016l.346-.36a.5.5 0 00-.016-.708l-1.43-1.367a4.274 4.274 0 10-3.57 1.923zm0-1.5a2.774 2.774 0 100-5.549 2.774 2.774 0 000 5.549z"></path>
  <path d="M6.931 9.046a.5.5 0 01.5-.5h6.435a.5.5 0 01.5.5v.6a.5.5 0 01-.5.5H7.431a.5.5 0 01-.5-.5v-.6zm.5 3.504a.5.5 0 00-.5.5v.6a.5.5 0 00.5.5h3.492a.5.5 0 00.5-.5v-.6a.5.5 0 00-.5-.5H7.431z"></path>
  <path fill-rule="evenodd" clip-rule="evenodd" d="M9.3 2.96v1.602h5.4V2.96H9.3zm-.5-1.5a1 1 0 00-1 1v2.602a1 1 0 001 1h6.4a1 1 0 001-1V2.46a1 1 0 00-1-1H8.8z"></path>
</svg>
```

### 6.3 加号节点选择弹层（`Div.ud__popover-content`）

- 参数来源：用户提供的 SnapSpec。弹层宽度固定为 `313px`，高度不得固定为捕获时的 `327px`，而应由当前可选 StageCard 数量自然撑开；两项状态的目标高度为 `143px`。
- 外层：`position:relative; overflow:hidden; min-width:30px; padding:12px 16px; box-sizing:border-box; background:#FFFFFF; border-radius:8px`。
- 边框：四边 `0.666667px solid #DEE0E3`。
- 阴影：`rgba(31,35,41,0.04) 0px 8px 24px 8px, rgba(31,35,41,0.04) 0px 6px 12px 0px, rgba(31,35,41,0.06) 0px 4px 8px -8px`。
- 字体：系统字体栈；`14px/22px/400`；正文 `#1F2329`。
- 标题：`margin-bottom:12px; color:#646A73`；文案为“可添加环节”。
- 弹层内部包含多个 `StageCard`，相邻项使用 `8px` 间距，最后一项 `margin-bottom:0`。普通动态选项弹层和仅含“结果复议处理”的单项专属弹层共用此规则，节点加入导致选项减少后不得留下固定高度空白。
- 稳定态运行时定位（2026-08-14 用户补充并修正）：弹层通过绝对定位层跟随当前加号，主体左边缘为 `addRect.right + 10px`。箭头使用下方原始 `8×16px` SVG，右边缘与主体左边框约重合 `1px`，箭头中心 y 始终对齐加号中心。箭头位置不是连续 clamp，而只有两种稳定态：中心距主体顶部 `16px`，或中心距主体底部 `16px`；下方空间足够时使用顶部态，下方不足时使用底部态，空间不足由弹层限高与内部滚动处理。弹层高度、画布/窗口滚动或窗口尺寸变化时重新定位，不得保留固定 `top:112px; left:calc(50% + 24px)`，也不得产生中间箭头位置。

```html
<svg width="8" height="16" viewBox="0 0 8 16" fill="none" xmlns="http://www.w3.org/2000/svg">
  <path d="M-.5 8v8h1c0-1.553.664-3.033 1.825-4.065l3.166-2.814a1.5 1.5 0 000-2.242L2.325 4.065A5.438 5.438 0 01.5 0h-1v8z"></path>
</svg>
```

### 6.4 流程连接与加号组件（`Div.style__Wrapper`）

- 组件尺寸：`16×41px`；`display:flex; flex-direction:column; align-items:center`。
- 连接线背景：`#BBBFC4`；连接线属于流程组件，不得使用 Header 分隔线样式替代。
- 加号默认态：外框 `16×16px`，白色背景，圆角 `999px`，四边 `0.666667px solid #BBBFC4`；加号 SVG `10×10px`，颜色 `#BBBFC4`。
- 加号打开态：外框与尺寸不变，边框和 SVG 颜色切换为 `#3370FF`，增加 `ud__popover-open` 状态类，并打开 6.3 弹层。
- 下方三角：`border-top:4.66667px solid #BBBFC4; border-right:3.33333px solid transparent; border-left:3.33333px solid transparent`。
- 组件字体：系统字体栈，`14px/21px/400`；加号图标字体尺寸 `10px/10px`。

## 7. 交互与接口边界

- 绩效管理员默认可进入；前端路由守卫和真实接口仍需后端授权校验。
- 上一步、下一步均保留。下一步先校验并调用真实保存接口，成功后进入内容设置；不传版本号。
- 保存接口采用 `PATCH /api/v1/performance/templates/{template_id}/workflow`（具体 URL 需与后端合同确认）；请求不得包含 version 字段。
- `usage_summary.cycle_count>0` 时顶部提示、可编辑范围和锁定字段由接口返回；前端不得写死“1”。附件未提供该提示的几何/颜色/换行，不能声称像素级。
- 必须建立新的蓝图版本；本文件版本 `v2-draft`，待用户确认后冻结。

## 8. 阻塞确认清单（不确认不进入编码）

1. 保存接口的最终 URL、响应结构、错误码及无版本号并发策略。

在以上问题得到明确答案或补充附件前，本蓝图只能用于需求澄清和测量，不可作为“像素级已确认”实现依据。
