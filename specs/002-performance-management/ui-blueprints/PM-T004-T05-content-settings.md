# PM-T004-T05 内容设置整体架构蓝图

状态：`confirmed for T05 only`（2026-08-21；弹层范围已拆分至 PM-T004-T06）。

来源：Perfect-Web-Clone-IDE 采集 session `idreamsky.feishu.cn_20260821_140802`（真实飞书绩效模板内容设置页，1920×1080）。该采集替代旧 `screenshot-20260819-112335.png`，作为 T05 页面结构与视觉依据。

## 范围

- 路由：`/performance/settings/templates/create?step=content`。
- 共享向导 Header 保持现有 56px 结构、步骤导航和上一步/下一步按钮；沿用 HR Portal 向导 Header，不还原飞书管理后台框架。
- 主体固定为三栏：左侧流程节点列表、中间内容配置区、右侧内容设置面板。
- 右侧未选择内容时显示“内容设置 / 暂未选择内容”空态。
- 本蓝图只冻结三栏页面、各环节中栏配置区与右栏空态。选择评估内容抽屉、新建菜单、三个新建弹窗及其交互统一以 [`PM-T004-T06-assessment-content-layers.md`](PM-T004-T06-assessment-content-layers.md) 为权威依据。

## 三栏几何基线（采集数据，1920 视口）

- 左栏（按评估流程配置 + 流程列表）：`320px` 固定，白底，右边界 1px 分隔线 `#DEE0E3`。
- 中栏（配置区）：弹性占剩余空间（`minmax(0, 1fr)`），背景 `#F5F6F7`，顶部 Tab 与内容卡片；1920 视口下为 `1280px`。
- 右栏（内容设置）：`320px` 固定，白底，`padding:20px 20px`，纵向可滚动。
- 三栏逻辑：左 320 固定 + 右 320 固定 + 中栏占剩余；页面为覆盖视口的全屏层，三栏铺满视口，最小内容宽度 `1300px`。

## 组件几何与视觉基线

### 左栏流程卡片

- 卡片 `280×64px`（容器宽 320，左右 padding 20），`padding:12px 16px`，边框 `#DEE0E3`，圆角 `8px`，卡片间距 `12px`。
- 标题 `14px / 21px`，执行人 `12px / 18px`，图标 `18×18px`。
- 顶部：`按评估流程配置`（14px/600/22px）+ `全部内容` 切换按钮。

### 中栏 Tab 与内容卡片

- Tab 当前项 `#3370FF`，底部 2px 指示线。
- 评估型/结果沟通：`配置填写内容`、`配置参考内容`、`更多`。
- 绩效结果查看：`配置查看内容`。
- 结果复议处理：`配置参考内容`。
- 内容卡片白底、圆角 `8px`，包含环节标准标题与拖拽点。

### 右栏空态

- 标题 `内容设置`：`14px / 600 / 22px`，`#1F2329`，左右内边距 `24px`，底部分隔线。
- 空态 `暂未选择内容`：距分隔线顶部 `212px`，居中，`14px / 400 / 22px`，`#8F959E`。

## 各环节中栏内容（采集确认）

| 环节 | 中栏 Tab | 卡片内容 |
|---|---|---|
| 绩效结果查看 | 配置查看内容 | “绩效结果查看” + “此区域应添加终评环节执行人「直属上级」填写的内容” + 添加终评内容 + 添加提示 + 添加其他参考内容 |
| 结果复议处理 | 配置参考内容 | “绩效结果复议处理” + “复议理由” + “这里展示被评估人发起复议时填写的内容” + 添加内容 + 添加提示 |
| 评估型环节 | 配置参考内容 | “设置评估尺度” + “评估型环节” + 添加内容 + 添加提示 |

## 交互边界

- 左侧节点列表由 `GET /api/v1/performance/templates/{template_id}/workflow` 返回的 `nodes` 按 `order` 驱动，不得静态写死；名称和执行人使用节点 API 字段，图标按节点类型映射。
- 上一步返回流程设置；下一步进入后续模板步骤，由父级向导控制。
- T05 不定义抽屉、菜单、弹窗、内容编辑器或真实保存行为，不得从本蓝图推导这些交互。

## 验收证据

- 采集 session：`C:\Users\gaby.liu\ClonedSites\idreamsky.feishu.cn_20260821_140802\`。
- 目标参考图归档：`PM-T004-T05-content-settings-reference.png`。
- 实现截图：`PM-T004-T05-content-settings-implemented.png`。采集原图不得作为实现截图，也不得提交未经审查的原始采集目录。

## 确认记录

- T05 三栏范围及上述几何已确认。
- 旧状态 `scope extended` 自本版本起失效；弹层部分必须单独确认 T06 蓝图后才能作为实现依据。
