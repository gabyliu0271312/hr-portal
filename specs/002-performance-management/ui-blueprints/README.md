# 绩效 UI 蓝图

这里存放绩效系统自己的页面和复杂流程蓝图。蓝图用于在编码前冻结信息架构、字段、按钮、角色可见性、状态、跳转和异常反馈。

`specs/011-universal-connector-platform/ui-blueprints/` 中的文件仅作为示意图和交互表达参考。只有任务实际涉及 UCP 时，才按根目录 `AGENTS.md` 要求读取 UCP 相关规格；绩效页面不归入 UCP 蓝图目录。

新页面、复杂向导、多角色工作台和多状态流程需要蓝图确认。小范围字段、文案、校验或样式调整只需更新对应功能的 `ui-interaction.md`。