# 功能规格目录

每个较完整的绩效业务能力使用一个 `PM-xxx-name` 目录。建议至少包含：

```text
PM-xxx-name/
├── spec.md
├── api-contract.md
├── data-model.md
├── ui-interaction.md
├── atomic-tasks.md
└── acceptance.md
```

不涉及的文件可以明确写“不涉及”，不要为了凑结构编造内容。