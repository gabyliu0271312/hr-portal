# PM-T015 UI 交互

completion_status: incomplete
pixel_restore_status: blocked
uncovered_reasons: missing_replayable_session, missing_target_screenshot, missing_interaction_evidence

- 页面入口：绩效后台 → 系统设置 → 考核方式设置。
- 顶部显示页面标题“考核方式设置”。
- 第一张卡片：关键指标考核、说明文案“在项目中让各成员制定并确认关键指标，并在绩效评估时考核指标完成情况；适用于 KPI（关键业绩指标）、PBC（个人绩效承诺） 等考核场景。”、查看使用说明、开启状态开关。
- 第二张卡片：项目制考核、说明文案“可在模板中设置由项目上级评估的环节，并在周期中导入各成员间的业务项目合作关系”、查看使用说明、开启状态开关。
- 项目制考核开关开启时，显示项目合作关系来源和项目团队绩效结果入口可见性设置；关闭时只保留项目制考核标题、说明文案、查看使用说明和开关。
- 项目制卡片中的“管理员在周期中手动导入”是已选禁用项；“导入时可选的项目角色”位于浅灰色摘要框内，显示 `--`，右侧为图标加“编辑”文字的编辑按钮。
- 项目卡片中的结果可见性模块：复选框位于“项目上级可查看「项目团队绩效结果」”之前，下面依次显示说明文案和“查看功能示例”。
- 点击“编辑”打开“设置项目角色”编辑弹窗；弹窗宽度 600px，包含标题、说明、空角色配置区、添加图标按钮、语言入口和“确定/取消”按钮。
- 弹窗说明文案为“在后台模板配置、前台筛选中选择「项目角色」时，角色顺序将与当前的顺序保持一致。项目角色支持修改名称，修改后，已引用角色的地方将同步展示最新名称。”
- 点击“添加角色”后，在按钮上方新增项目角色输入行；输入框复用 `PerformanceTextField` 的 `feishu-input` 变体，显示占位文案“请输入中文角色名称”和“中文”语言标签；添加按钮移动到输入行下方。
- 当角色行达到两行及以上时，按采集参数显示共享拖拽手柄和共享删除按钮，文本框相应变短；单行时两侧控件隐藏，不占用输入框宽度。
- 删除按钮的 hover 底色使用统一设计 token `--color-surface-disabled`；删除仅移除当前本地输入行，不发送网络请求。
- 角色输入行仅维护弹窗内本地状态；未提供角色保存 API，“确定”和“取消”仅关闭弹窗，不发送网络请求。
- 关键指标考核开关支持后端读取和保存；加载或保存期间该开关禁用，接口失败时回滚当前状态并展示可重试错误提示。
- 项目结果可见性复选框仍仅维护本地状态；未提供保存 API，因此不发送网络请求。
- 页面沿用现有 PerformanceAdminLayout 和系统设置导航。
- 关键指标考核开关与后台侧边栏联动：开启时，在“席位管理”后显示“指标管理”可折叠菜单，包含“指标库”和“指标模板”两个占位入口；关闭时隐藏该菜单。
- “指标管理”菜单复用现有后台侧边栏的折叠/展开、hover、active 和子菜单交互；菜单图标使用 18×18px 的 `MetricManagementOutlined` SVG。
