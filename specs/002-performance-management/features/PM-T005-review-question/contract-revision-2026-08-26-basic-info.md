# PM-T005 Basic Information Contract Revision 2026-08-26

## Status

Confirmed contract supplement for the Basic Information card. This document refines the implementation contract and does not change source capture evidence.

## 1. Field label and option text contract

Every parameter below is explicitly labelled with its evidence source and status. These values apply to the visible field labels (`语言`, `名称`, `描述`, `类型`) and the type option text.

| parameter label | value | source | status |
|---|---|---|---|
| `field-label.color` | `rgb(31,35,41)` | target computed-style capture, user-provided output | confirmed |
| `field-label.font-size` | `14px` | target computed-style capture, user-provided output | confirmed |
| `field-label.line-height` | `22px` | target computed-style capture, user-provided output | confirmed |
| `field-label.font-weight` | `600` | target computed-style capture, user-provided output | confirmed |
| `required-mark.font-weight` | `400` | target required-mark style contract | confirmed |
| `required-mark.color` | `rgb(245,74,69)` | target computed-style capture | confirmed |

The previous `font-weight: 400` and `line-height: 21px` values in this document were not the field-label values and are superseded. They must not be used for implementation.

The type information icon is part of the option affordance. Its icon name, SVG path, viewBox, and geometry are separate parameters and must be sourced from the SVG registry; an unobserved value is `TBD(blocked)`, never an inferred substitute.

## 2. Description control contract

The measured target is the complete textarea control area, including its wrapper and word counter, not only the native textarea element:

| parameter label | value | source | status |
|---|---|---|---|
| `description.outer.x` | `581px` | `192219_038154` target contract | confirmed |
| `description.outer.y` | `359px` | `192219_038154` target contract | confirmed |
| `description.outer.width` | `759px` | `192219_038154` target contract | confirmed |
| `description.outer.height` | `49.33px` | `20260826_174927_786103/step_00/checkpoint/target_contract.json` | confirmed for normal create state |
| `description.inner.native-height` | `49.33px` | `20260826_174927_786103/step_00/checkpoint/target_contract.json` | confirmed for normal create state |
| `description.counter.text` | `/1000` | target validation/interaction evidence | confirmed |

The outer control, inner textarea, padding, and counter must be measured and implemented as one stable component. `rows` defaults must not override the measured height.

The earlier `107px` value was a mixed-session wrapper measurement and is not the normal create-state textarea height. It must not be used to size the native textarea or derive the type-row position. The normal create-state target contract records a `49.33px` textarea bbox; validation messages are a separate state and may increase the form-item/card height.

## 3. BasicInfoCard surface contract

| parameter label | value | source | status |
|---|---|---|---|
| `basic-info-card.x` | `556px` | `192927_379131` target contract | confirmed |
| `basic-info-card.y` | `74px` | `192927_379131` target contract | confirmed |
| `basic-info-card.width` | `800px` | `192927_379131` target contract | confirmed |
| `basic-info-card.height` | `388px` | `192927_379131` target contract | confirmed |
| `basic-info-card.background` | `rgb(255,255,255)` | target computed-style capture | confirmed |
| `basic-info-card.border` | `none` | target computed-style capture | confirmed |
| `basic-info-card.radius` | `8px` | target computed-style capture | confirmed |

The card shadow must use the captured three-layer shadow from `pixel-contract.md`; an approximate shadow is not considered equivalent. Card height and internal spacing must not be generated by independent form default margins.

The fixed `388px` card height includes the complete BasicInfoCard content, including the type label and all four type options. Type controls and labels must remain inside the card's content box at the captured viewport. A child form or control must not extend beyond the card boundary; accumulated `el-form`/`el-form-item` defaults must be normalized within the card rather than allowed to create overflow.

## 4. Type radio contract

Each type option control must have a stable `16x16px` radio geometry:

| parameter label | value | source | status |
|---|---|---|---|
| `type-radio.width` | `16px` | `192219_038154` computed | confirmed |
| `type-radio.height` | `16px` | `192219_038154` computed | confirmed |
| `type-radio.selected-fill` | `rgb(20,86,240)` | `192219_038154` computed | confirmed |
| `type-radio.checked-ink` | `rgb(255,255,255)` | `192219_038154` computed | confirmed |
| `type-radio.shape` | `circular` | `192219_038154` computed | confirmed |
| `type-radio.label.font-size` | `14px` | target computed-style capture | confirmed |
| `type-radio.label.line-height` | `22px` | target computed-style capture | confirmed |
| `type-radio.label.color` | `rgb(31,35,41)` | target computed-style capture | confirmed |

Target x positions remain `577px`, `715px`, `856px`, and `966px` at the captured viewport. Element Plus default radio dimensions are not authoritative.

## 4.1 Name input contract

| parameter label | value | source | status |
|---|---|---|---|
| `name-input.x` | `588px` | `192927_379131` selected-state target contract | confirmed |
| `name-input.y` | `243px` | `192927_379131` selected-state target contract | confirmed |
| `name-input.width` | `695px` | `192927_379131` selected-state target contract | confirmed |
| `name-input.height` | `28px` | `192927_379131` selected-state target contract | confirmed |

The name input must not inherit the description control's `759px` width or a default `32px` height.

## 5. Acceptance evidence

- Verify the complete control bbox, not a child node bbox.
- Verify default and selected type states separately.
- Verify card surface and shadow at `1920x1080`.
- If any value is not supported by source evidence, record `blocked` or `N/A`; do not infer a replacement value.

## Implementation boundary

This revision applies only to PM-T005 Basic Information UI components. It does not authorize API, persistence, permission, migration, UCP, or unrelated global style changes.

## Evidence addendum: same-state geometry and resize

The collector session `C:\Users\gaby.liu\ClonedSites\idreamsky.feishu.cn_20260826_185525_429469` (`hybrid-000-after`) is the authoritative normal create state for field geometry. It records name `759x31` at `(581,238)`, description `759x49` at `(581,319)`, and remark `759x49` at `(581,729)`. The contract consequently requires equal widths for name, description, and remark; the former `695px` name width is retired as cross-state evidence.

Computed style evidence confirms vertical resize capability (`resize: vertical`, min-height `49.3333px`, very large max-height) for both textareas. The attempted realtime recording has zero trusted events (`recording_event_count=0`), so `dragging` and `after-drag` states are explicitly `not_observed/blocked` and must not be implemented from guessed heights.
