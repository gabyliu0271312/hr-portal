# PM-001 UI 与交互边界

## 当前结论

PM-001 先确认授权模型和后端授权契约，不直接实现新的业务 UI。

后续如实现以下页面，必须先在 `../../ui-blueprints/` 生成并获得确认：

- 独立绩效超级管理员登录页；
- 绩效管理员账号管理页；
- 角色、范围、校准组和动态身份配置页；
- HRBP 结果发布转交界面。

每个页面蓝图必须表达：入口、角色可见性、字段、按钮、危险操作确认、加载态、空态、错误态、无权限态和审计提示。