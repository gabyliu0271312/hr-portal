# PM-T004 原子任务

- [ ] PM-T004-T01 冻结模板流程 DTO、节点枚举与真实 API 契约
  - Goal: 提供 GET/PATCH 流程接口及稳定错误码，兼容旧节点读取映射。
  - Type: contract/backend/API
  - Prerequisites: PM-T002 权限入口；本规格确认
  - Required reading: `spec.md`、`workflow-design.md`、`data-model.md`、`permission-model.md`、`references/execution-contract.md`
  - Allowed modifications: `hr-portal/backend/app/performance/**`、`hr-portal/frontend/src/api/performance.ts`、对应测试；允许新增 `specs/002-performance-management/features/PM-T004-template-create/**`
  - Forbidden modifications: 已有 migration、周期业务、UCP 模块
  - Input contract: template_id、节点 JSON、幂等请求标识
  - Output contract: 规范化节点、usage_summary、editable_scope、错误码
  - UI: no direct UI; defines route payload consumed by T02/T03
  - API/database/permission/external-system impact: template/workflow persistence, `template.manage`, audit; no external system
  - Test contract: Given valid/invalid/locked/forbidden/concurrent payload; When GET/PATCH; Then exact DTO/error/audit behavior
  - Acceptance: API schema, permission allow/deny, locked-field rejection, reopen consistency
  - Evidence: API tests, DTO examples, audit assertion
  - Blockers: executor option search contract; no-version idempotency strategy
  - Non-scope: UI rendering and content settings
  - Definition of done: contract and tests complete; only main agent may check task

- [ ] PM-T004-T02 实现流程设置画布与节点配置面板
  - Goal: 按蓝图实现附件节点、连线、加号、选中/删除和右侧表单。
  - Type: frontend
  - Prerequisites: PM-T004-T01、蓝图 v2-draft 获用户视觉确认
  - Required reading: `spec.md`、`ui-interaction.md`、`PM-T004-template-flow-settings.md`、Spec 012 UI 守则
  - Allowed modifications: `hr-portal/frontend/src/views/performance/PerformanceTemplateWorkflowSettings.vue`、同名 spec、必要路由和样式文件
  - Forbidden modifications: PerformanceLayout 结构、周期页面、无关全局主题
  - Shared files and merge rule: router changes serial with PM-T004-T03
  - Input contract: GET DTO and editable_scope
  - Output contract: PATCH request body; route navigation events
  - UI: route `/performance/settings/templates/create?step=workflow`; states loading/empty/selected/locked/error/forbidden/success; blueprint `PM-T004-template-flow-settings.md` v2-draft; PNG `PM-T004-T02-template-flow-settings.png`
  - API/database/permission/external-system impact: no direct schema; consumes permission and API contracts
  - Test contract: Given each captured state; When click/edit/add/delete/next/back; Then DOM state and payload match blueprint
  - Acceptance: 1920x959 screenshot comparison, keyboard focus, disabled locked fields, error preservation
  - Evidence: Vitest, Playwright screenshots, build output
  - [x] PM-T004-T02-F01 修复流程节点名称被内部文本宽度约束提前裁切，使节点先随内容自适应增长。证据：`PerformanceTemplateWorkflowSettings.spec.ts` 2 项通过、`vue-tsc --noEmit` 通过、Docker 生产构建通过。
  - [x] PM-T004-T02-F02 按 StageBox SnapSpec 修复图标/标题/执行人两行结构、原始 SVG 映射及达到父级宽度上限后的省略号。证据：专项 Vitest 9 项通过、`vue-tsc --noEmit` 通过、Docker 生产构建通过；运行容器产物含标题行 flex、2 处省略号规则和完整结果查看 SVG path。
  - [x] PM-T004-T02-F03 实现加号 hover/focus 打开弹层、节点 hover/focus 显示删除按钮，并替换为 `DeleteTrashOutlined` 16×16 SVG。证据：专项 Vitest 11 项通过、`vue-tsc --noEmit` 通过、Docker 生产构建通过；运行容器产物包含 hover/focus 删除规则和原始垃圾桶 path。
  - [x] PM-T004-T02-F04 修复最后 1 个评估型环节可被删除的回归：UI 隐藏删除入口，删除方法同步防护。证据：专项 Vitest 12 项通过（覆盖 1→2→1 数量状态）、`vue-tsc --noEmit` 通过、Docker 生产构建和容器替换通过。
  - [x] PM-T004-T02-F05 修复节点 hover 未高亮、节点与删除按钮之间 4px hover 断点、删除按钮 hover 灰底缺失。证据：专项 Vitest 13 项通过、`vue-tsc --noEmit` 通过、Docker 生产构建和容器替换通过；运行产物包含 hover=selected 高亮、4px 透明命中桥和 `#1f232933`（即 rgba(31,35,41,0.2)）按钮 hover 背景。
  - [x] PM-T004-T02-F06 曾按当时确认实现“选中节点持续显示删除按钮”；该交互随后被 PM-T004-T02-F07 的最新用户确认废止，当前实现不得再使用 `.selected` 控制删除按钮可见性。历史证据：专项 Vitest 14 项通过、`vue-tsc --noEmit` 通过、Docker 生产构建和容器替换通过。
  - [x] PM-T004-T02-F07 按最新确认将删除按钮收敛为仅节点 hover 时显示：选中、新增后自动选中和焦点状态均不得持续显示；保留连续 hover 命中桥及不可删除节点保护。证据：专项 Vitest 14 项通过、`vue-tsc --noEmit` 通过、Docker 生产构建和容器替换通过；页面返回 HTTP 200，运行镜像 `sha256:24e6e99f86432eea33ae31b96115a4f167ebe6a4591cbdb7e5d1c0e5e906b03f`，运行 CSS 的删除按钮显示选择器仅有 `.stage-node:hover .delete-node`。
  - [x] PM-T004-T02-F08 限制除评估型环节外的可新增环节各最多存在 1 个：已有类型从弹层隐藏，删除后恢复；评估型环节保持可重复添加，并在添加方法内防止绕过 UI 重复创建。证据：专项 Vitest 21 项通过、`vue-tsc --noEmit` 通过、Docker 生产构建和容器替换通过；页面返回 HTTP 200，运行镜像 `sha256:3cca212045e6859517a0f3125e435258c6e3d5380caece56dfbaa0c3f7faef66`，运行 JS 产物包含 `type === evaluation || !businessNodes.some(node_type === type)` 等价过滤逻辑。
  - [x] PM-T004-T02-F09 新增 `result_reconsideration`“结果复议处理”：只允许从绩效结果查看后的专属加号添加，专属弹层只显示该项，添加后相关连接器不再显示加号；弹层/画布复用用户提供 SVG 并分别统一为 16/20px。证据：专项 Vitest 25 项通过、`vue-tsc --noEmit` 通过、Docker 生产构建和容器替换通过；页面返回 HTTP 200，运行镜像 `sha256:91ba42c1113bcfd36dfb35f4578633c665f786e523478485a96504ab8fb31dbd`，运行 JS/CSS 产物包含 `result_reconsideration`、用户提供 SVG 起始 path 和弹层图标样式。
  - [x] PM-T004-T02-F10 修复可添加环节动态减少后弹层保留固定高度空白：普通弹层与“结果复议处理”单项弹层统一固定宽 `313px`、高度按内容自适应，最后一个 StageCard 不保留底间距。UI 要求：遵循 `ui-interaction.md`、蓝图 6.3、Spec 012 N0001/N0002/N0004/N0005 与 N1102-N1104；不改变 Header、节点画布和弹层宽度。测试要求：组件测试覆盖普通多项和结果复议单项状态、`vue-tsc --noEmit`、Docker 生产构建及运行产物检查。验收：动态选项减少后无长空白，单项弹层同样自然收高；正常 hover 打开和节点新增逻辑不回归。证据：专项 Vitest 26/26 通过、`vue-tsc --noEmit` 通过、Docker 生产构建与容器替换通过；目标路由 HTTP 200，运行镜像 `sha256:f6d437f9d3772bb53f1fe83c0b33f46130868f140e4e60ab17ac2d3cdb7b397d`，运行 CSS 含 scoped `.popover-stage:last-child{margin-bottom:0}` 且不含 `height:327px`。
  - [x] PM-T004-T02-F11 将可添加环节弹层改为加号锚点动态定位并补齐左侧箭头：主体固定在加号右侧 `10px`；箭头复用目标 `8×16px` 原始 SVG，中心对齐加号且只允许“距顶部 16px”或“距底部 16px”两个离散位置，下方空间不足时从顶部态切换为底部态，空间不足由弹层限高与内部滚动处理；滚动、缩放和动态高度变化时重新定位。UI 要求：遵循 `ui-interaction.md`、蓝图 6.3、Spec 012 N0001/N0002/N0004/N0005 与 N1102-N1104，不改变 Header/节点布局/增删规则。测试要求：组件测试覆盖顶部态、底部态、原始 SVG、普通/结果复议弹层及现有 hover 交互；执行 `vue-tsc --noEmit`、Docker 生产构建和运行产物检查。验收：弹层不再使用固定 `top/left`，箭头不得产生中间位置，在各连接器位置始终指向当前加号，滚动/尺寸变化后保持对齐。证据：专项 Vitest 27/27 通过、`vue-tsc --noEmit` 通过、Docker 生产构建与容器替换通过；目标路由 HTTP 200，运行镜像 `sha256:0503246e2ca43b8d168e3422595eb3402574a3813832c18d47ee0fd49301f6e5` 且保持 `hr-portal_default` 网络；运行 JS/CSS 含原始箭头 path、动态 `translate3d`、外层 `overflow:visible` 与内部滚动层，不含旧 `top:112px` 固定定位。
  - Blockers: final executor option labels
  - Non-scope: content editor implementation
  - Definition of done: implementation, UI evidence and tests complete; only main agent may check task

  - [x] PM-T004-T02-F12 新增右侧面板公共标准标题栏：按 `319×48px`、`padding:12px 20px`、`16px/600/24px`、`rgba(0,0,0,.65)` 和底部分隔线参数实现；标题使用节点类型标准名称，不随用户修改的节点名称变化。证据：专项 Vitest 28/28 通过（覆盖自定义名称与标准标题分离）、`vue-tsc --noEmit` 通过；Docker 构建与容器替换通过，目标路由 HTTP 200，运行镜像 `sha256:6987b2dfaa5eaff1bcd0cff7bb984a82a36d7f5ccd3449e6e378b5f2d6f3207e`，保持 `hr-portal_default` 网络。
  - [x] PM-T004-T02-F13 修复右侧内部面板因 Windows 原生滚动条占位导致的 `309.3333px` 宽度：保留面板滚动能力，隐藏滚动条占位，使内容区域对齐目标 `.p-12` 的 `319.3333px`。证据：专项 Vitest 29/29 通过、`vue-tsc --noEmit` 通过；Docker 构建与容器替换通过，目标路由 HTTP 200，运行镜像 `sha256:741d7d1b106297550007a43320b2b8061c36f37191fe188af3f4042be8909dab`，保持 `hr-portal_default` 网络。
  - [x] PM-T004-T02-F14 按目标 DOM 结构拆分右侧标题与表单滚动区域：外层 `overflow:hidden`，标题保持静态，`.panel-scroll` 独立 `overflow:auto`，不使用 `position:sticky`。证据：专项 Vitest 29/29 通过、`vue-tsc --noEmit` 通过；Docker 构建与容器替换通过，目标路由 HTTP 200，运行镜像 `sha256:8e82eabe331eaf05bfe2e6eefacd283a19e21792a6df212929349d86fc8c4da4`，保持 `hr-portal_default` 网络。

- [ ] PM-T004-T03 接入模板流程保存、上一步/下一步与已有周期只读策略
  - Goal: 真实 API 保存成功后导航，失败可重试，按 editable_scope 裁剪编辑能力。
  - Type: frontend/API integration
  - Prerequisites: PM-T004-T01、PM-T004-T02
  - Required reading: `spec.md`、`ui-interaction.md`、`performance.ts`、权限守卫实现
  - Allowed modifications: `hr-portal/frontend/src/api/performance.ts`、模板创建路由/视图测试
  - Forbidden modifications: backend migration、周期快照语义、内容设置后端
  - UI: same route; impact banner; next/back transitions; independent PNG `PM-T004-T03-template-flow-settings-used-cycle.png`
  - API/database/permission/external-system impact: PATCH workflow, 403/409/422 feedback, audit observed through API
  - Test contract: Given used-cycle and draft templates; When save/back/next; Then locked fields, payload and navigation are correct
  - Acceptance: UI->API->reopen trace, permission deny, concurrent conflict, no data loss on failure
  - Evidence: E2E trace, request fixture, screenshot pair
  - Blockers: backend endpoint availability
  - Non-scope: versioning, publish, content editing
  - Definition of done: integration and evidence complete; only main agent may check task

- [ ] PM-T004-T04 流程设置视觉与验收回归
  - Goal: 建立附件 48 个状态的 focused visual regression。
  - Type: test/acceptance
  - Prerequisites: PM-T004-T02、PM-T004-T03
  - Required reading: `acceptance-evidence.md`、blueprint v2-draft
  - Allowed modifications: `hr-portal/frontend/src/views/performance/*WorkflowSettings*.spec.ts`、`specs/002-performance-management/ui-blueprints/PM-T004-T04-*.png`
  - Forbidden modifications: 业务实现和 API 契约
  - UI: compare initial + state-1..47 at 1920x959; independent PNG per task
  - Test contract: Given deterministic fixtures; When Playwright captures each state; Then threshold and critical element assertions pass
  - Acceptance: success, empty, error, locked, permission and navigation states covered
  - Evidence: test output and PNG manifest
  - Blockers: production font availability
  - Non-scope: backend load/performance tests
  - Definition of done: all required evidence recorded; only main agent may check task

  - [x] PM-T004-T02-F15 Extract the shared performance switch and workflow-node basic fields into reusable performance components. Evidence: `PerformanceSwitch.spec.ts` 2/2, `WorkflowNodeBasicFields.spec.ts` 2/2, workflow settings regression 29/29, and `vue-tsc --noEmit` passed. Presentation/component reuse only; API DTO and workflow persistence contract unchanged.
  - [x] PM-T004-T02-F16 Replaced the native executor select with the SnapSpec-aligned custom selector structure, 263.333x32 geometry, 220x28 content/search layer, 12x12 DownBoldOutlined arrow, open filtering, and option list. Evidence: `PerformanceExecutorSelect.spec.ts` 2/2, focused workflow regression 29/29, `vue-tsc --noEmit`, Docker build, and route HTTP 200.
  - [x] PM-T004-T02-F17 Calibrated the executor dropdown from target runtime measurements: `263.323x165.333px`, top-start placement with `translateY(-90px)`, z-index `1050`, 30px option rows, `margin:1px 3px`, `padding:4px 8px`, and selected-state background/check icon. Evidence: focused selector tests, workflow regression, type check, Docker build, and route HTTP 200.
  - [x] PM-T004-T02-F18 Corrected executor selector interaction: pointer entry opens the list, hover styling follows the pointer instead of persisting on the selected option, and the dropdown is always rendered below the selector. Evidence: `PerformanceExecutorSelect.spec.ts` 3/3, workflow regression 29/29, type check, Docker build, and route HTTP 200.
  - [x] PM-T004-T02-F19 Restored the selected executor row's initial target background while preserving pointer-following hover behavior: the checked row is shaded only until another option is hovered, then the shade moves to the hovered row. Evidence: `PerformanceExecutorSelect.spec.ts` 3/3, workflow regression 29/29, `vue-tsc --noEmit`, Docker build, and route HTTP 200.
  - [x] PM-T004-T02-F20 Aligned the executor field label with the target `ud__row`: independent semibold title, SimSun required mark, and an 8px label-to-selector gap with 20px row spacing. Evidence: workflow regression 29/29, `vue-tsc --noEmit`, Docker build, and route HTTP 200.
  - [x] PM-T004-T02-F21 Enforced the subject-executor workflow rule: selecting `被评估人` normalizes evaluation to `SINGLE`, resets final-result to off, disables both evaluation choices and the final-result switch, and exposes the captured 138ms top tooltip with the original 16x8 arrow and restriction copy. Switching away restores editing with the single/off defaults. Evidence: workflow regression 30/30, shared selector/switch tests 5/5, `vue-tsc --noEmit`, Docker build, and route HTTP 200.
  - [x] PM-T004-T02-F22 Corrected evaluation-type radio alignment by centering the 16px control and its 6px ink inside the captured 22px text row, while retaining the 8px control-to-label gap and disabled subject-executor styling. Evidence: workflow regression 30/30, `vue-tsc --noEmit`, Docker build, and route HTTP 200.
  - [x] PM-T004-T02-F23 Added the `360°评估人` fixed rule alongside the subject rule: selecting it normalizes evaluation to `MULTI`, resets and disables final-result, locks both radio choices, and reuses the measured restriction tooltip with executor-specific copy. Returning to another executor restores the current single/off editable defaults. Evidence: workflow regression 30/30, shared selector/switch tests 5/5, `vue-tsc --noEmit`, Docker build, and route HTTP 200.
  - [x] PM-T004-T02-F24 Added the `实线上级` fixed-single rule while keeping final-result editable. Enabling final-result renders the captured 263px, 14/20, 4px-top-gap helper text; switching to another executor reapplies its current default state. Evidence: workflow regression 30/30, shared selector/switch tests 5/5, `vue-tsc --noEmit`, Docker build, and route HTTP 200.
  - [x] PM-T004-T02-F25 Added the `虚线上级` split rule: evaluation type remains editable, while final-result is normalized off, disabled, and explains the restriction through the measured executor-specific tooltip. Evidence: workflow regression 30/30, shared selector/switch tests 5/5, `vue-tsc --noEmit`, Docker build, and route HTTP 200.
  - [x] PM-T004-T02-F26 Replaced the native real-line-manager checkboxes with the captured custom checkbox structure and exact option-panel geometry: `263.333x136px`, `12px` padding, `8px` row gaps, `16px` controls, unchecked `#8F959E` border, checked `#0442D2` fill, and the original `12x12px` white check path. Existing `executor_types` binding and lock behavior are preserved. Evidence: workflow regression 31/31, shared selector/switch tests 5/5, `vue-tsc --noEmit`, Docker production build and container replacement passed; target route HTTP 200, running image `sha256:a9c44a407f7b288b363f0744e1d6172e416b6a481692e94bbde590ced26818d9`.
  - [x] PM-T004-T02-F27 Implemented the `360°评估人`-only invitation configuration and persistence contract: shared 28×16 switch, `ALL`/`PARTIAL` scope, deduplicated candidates from other evaluation nodes, node-scoped configuration retention, stale-role cleanup, cross-360 validation with `请选择允许邀请的执行人角色`, and real `GET/PATCH` reopen behavior backed by `performance_template_workflows`. Evidence: frontend focused regression 44/44 and `vue-tsc --noEmit` passed; backend focused tests 11/11 passed; Alembic `0205` upgrade/downgrade/upgrade passed; real API acceptance returned GET 200, PATCH 200, reopen GET 200 and matched the three invitation fields in JSONB; Docker backend/frontend production builds, container replacement, route HTTP 200, unauthenticated API 401, and migration head checks passed. Independent implemented-state PNG: `../../ui-blueprints/PM-T004-T02-F27-implemented-360-invite-executors.png`. Spec 012 UI checks applied: N0001/N0002/N0004/N0005 and N1102-N1104; no UCP configuration or forbidden parallel design system was introduced.
  - [x] PM-T004-T02-F28 Restored the measured empty-candidate interaction for `部分执行人`: clicking the still-enabled role selector retains its internal open/focus state but renders no dropdown, option row, or empty-state popover; the normal `#D0D3D6` border is retained and `aria-expanded` is omitted when no listbox exists. Non-empty candidate behavior remains unchanged. Evidence: target runtime reported `popupAppeared=false`, zero visible overlays/options, and an open selector with focused search input; component and workflow focused regression 39/39 plus `vue-tsc --noEmit` passed; Docker frontend production build and container replacement passed; runtime DOM verified `rootOpen=true`, `hasOptions=false`, `dropdown=false`, `optionCount=0`, and `ariaExpanded=null`. Independent implemented-state PNG: `../../ui-blueprints/PM-T004-T02-F28-implemented-empty-candidate-no-popup.png`. Spec 012 UI checks applied: N0001/N0002/N0004/N0005 and N1102-N1104; no API, persistence, permission, UCP, or information-architecture change.
  - [x] PM-T004-T02-F29 Reused the evaluation-node executor control for result-communication nodes while constraining the primary selector to `实线上级` and `虚线上级`. `实线上级` retains the four manager-level checkboxes and node-scoped selections; `虚线上级` hides them. Result-communication nodes do not inherit evaluation type, invitation settings, or the final-result switch. New and legacy nodes normalize to the supported real-line default without changing the workflow API schema. The shared dropdown now derives its height from the rendered option count and only applies the captured `165.333px` maximum, so the two-option result-communication list no longer retains the five-option empty area. Evidence: workflow settings regression 37/37, executor selector regression 4/4, combined focused regression 41/41, `vue-tsc --noEmit`, Docker frontend production build and container replacement, route HTTP 200, runtime verification of the content-driven two-option list, and production-asset verification of the `height:auto`, `2px` vertical padding, and `165.333px` cap contract that yields the target `69.333px` two-option geometry. Independent implemented-state PNG: `../../ui-blueprints/PM-T004-T02-F29-implemented-result-communication-executor.png`. Spec 012 UI checks applied: N0001/N0002/N0004/N0005 and N1102-N1104; no backend DTO, migration, permission, UCP, or information-architecture change.
  - [x] PM-T004-T02-F30 Added the fixed work-summary executor field through reusable executor-domain components. `performanceExecutorOptions.ts` now provides the canonical `{ type: 'SUBJECT', label: '被评估人' }` definition shared by the evaluation selector and fixed executor presentation; `PerformanceExecutorTag.vue` owns the neutral executor token and `WorkflowFixedExecutorField.vue` owns the required field composition. Work-summary creation and workflow hydration/save normalize to `executor_label='被评估人'` and `executor_types=['SUBJECT']` without introducing a new API field. Runtime geometry matches the captured target: field `271×54px`, title `70×22px`, tag `68×24px`, `rgba(31,35,41,.1)` background, and `4px` radius. Evidence: component/workflow focused regression 45/45, `vue-tsc --noEmit`, Docker frontend production build and container replacement, route HTTP 200, and independent implemented-state PNG `../../ui-blueprints/PM-T004-T02-F30-implemented-work-summary-executor.png`. Spec 012 UI checks applied: N0001/N0002/N0004/N0005 and N1102-N1104; no backend DTO, migration, permission, UCP, external-system, or information-architecture change.
  - [x] PM-T004-T02-F31 Add the 360-invitation fixed subject executor and persisted previous-node prerequisite setting.
    - Goal: `reviewer_360_invite` reuses the fixed `SUBJECT / 被评估人` executor field and persists a default-off `require_previous_node_completion` setting whenever a previous business node exists.
    - Type: frontend/API contract/backend normalization/test/documentation.
    - Prerequisites: PM-T004-T02-F15, PM-T004-T02-F27, PM-T004-T02-F30.
    - Required reading: feature `spec.md`, `ui-interaction.md`, `performance.ts`, `templates_router.py`, `template_workflow_service.py`, Spec 012 N000/N00/N11, and the supplied SnapSpec/runtime measurements.
    - Allowed modifications: `hr-portal/frontend/src/components/performance/*`, `hr-portal/frontend/src/views/performance/PerformanceTemplateWorkflowSettings.vue`, its focused specs, `hr-portal/frontend/src/api/performance.ts`, `hr-portal/backend/app/performance/templates_router.py`, `template_workflow_service.py`, `hr-portal/backend/tests/test_performance_template_workflow.py`, this feature directory, and one independent F31 PNG.
    - Allowed new files: reusable performance-domain info-popover/switch-setting-row Vue components and focused component specs; `specs/002-performance-management/ui-blueprints/PM-T004-T02-F31-implemented-360-invite-prerequisite.png`.
    - Forbidden modifications: cycle/project schema, cycle workflow snapshots, node-task models, runtime task-entry routes, permissions, UCP, external systems, and unrelated node business behavior.
    - Shared files and merge rule: preserve unrelated dirty-worktree changes; only add the new workflow-node field and scoped normalization to shared DTO/service files.
    - Input contract: `require_previous_node_completion: boolean`, default `false`; only `reviewer_360_invite` with an immediately preceding business node may persist `true`.
    - Output contract: GET/PATCH returns the normalized boolean; `reviewer_360_invite` always returns `executor_label='被评估人'` and `executor_types=['SUBJECT']`; first-business-node and non-invitation nodes normalize the boolean to `false`.
    - UI: existing workflow route; reuse `WorkflowFixedExecutorField`, `PerformanceExecutorTag`, and `PerformanceSwitch`; extract shared `PerformanceInfoPopover` and `PerformanceSwitchSettingRow`. Setting row is `263.333×22px`, label `196×22px`, icon `16px` with `4px` left gap, switch `28×16px` with `8px` left gap. Tooltip opens after about `124ms`, prefers top placement with a `10px` gap, measures `420×69.333px`, keeps a `16px` viewport margin, and aligns its `16×8px` arrow to the icon center. No full-page blueprint is required because information architecture is unchanged; independent implemented PNG is mandatory.
    - API/database/permission/external-system impact: existing GET/PATCH workflow DTO and JSONB only; no migration, new endpoint, permission, UCP, or external-system impact.
    - Test contract:
      - Given: first and non-first 360-invitation nodes, work-summary nodes, non-invitation nodes, locked templates, and true/false PATCH payloads.
      - When: create, hydrate, toggle, save, and reopen workflow nodes.
      - Then: executor components are shared, first-node setting is absent/false, non-first state round-trips, unsupported nodes cannot retain true, and locked state disables the switch.
      - Test files: shared component specs, `PerformanceTemplateWorkflowSettings.spec.ts`, and `test_performance_template_workflow.py`.
      - Commands: focused Vitest, focused pytest, `vue-tsc --noEmit`, frontend production build, route/DOM verification.
      - skipped/not-run rule: any unrun runtime-task test is explicitly out of scope, not passed.
    - Acceptance: UI -> PATCH payload -> JSONB service normalization -> GET reopen; component reuse visible in imports/render tree; tooltip geometry and accessibility verified; no runtime blocking claim.
    - Evidence: shared component/workflow focused Vitest 46/46 passed; backend workflow pytest 13/13 passed; `vue-tsc --noEmit` and frontend production build passed. The UI-to-PATCH tests cover non-first-node `true` round-trip and first-node forced `false`; service tests cover fixed `SUBJECT / 被评估人` normalization and rejection of the flag on unsupported nodes. Independent implemented-state PNG: `../../ui-blueprints/PM-T004-T02-F31-implemented-360-invite-prerequisite.png`. Spec 012 UI checks applied: N0001/N0002/N0004/N0005 and N1102-N1104; the local preview route was login-guarded, so the PNG is an isolated implemented-state visual render rather than an authenticated runtime-route capture. No runtime prerequisite enforcement is claimed.
    - Blockers: none for the confirmed template-only scope.
    - Non-scope: cycle template binding, cycle-node snapshots, node-task generation/status, task-entry API, and enforcement of the configured prerequisite.
    - Definition of done: development, UI, API round-trip, tests, screenshot, and evidence complete; only then mark `[x]`.
  - [x] PM-T004-T02-F32 Extract the selectable workflow executor field into `WorkflowExecutorField.vue` and reuse it for evaluation, result-communication, and 360-confirmation nodes. 360-confirmation supports only `实线上级`/`虚线上级`; its real-line levels are limited to `直属上级`, `隔 1 级上级`, and `隔 2 级上级`, with frontend and backend workflow normalization using the existing JSONB contract. Non-scope: runtime task behavior, migration, permission, and external systems. Evidence: focused frontend Vitest 44/44, backend pytest 15/15, `vue-tsc --noEmit`, host/Docker production builds, and frontend/backend container replacement passed; route HTTP 200; running frontend image `sha256:d730b55bb82499ca1435d3c3c11c51ff6564bc86fd5152e4e774591f65314015`, backend image `sha256:80588db55096b522c9e7185afa13583943791b70aec00de42b104092f2fe662c`; independent isolated implemented-state PNG `../../ui-blueprints/PM-T004-T02-F32-implemented-360-confirmation-executor.png`.
  - [x] PM-T004-T02-F33 Reuse `WorkflowFixedExecutorField.vue` for calibration nodes and reserve the template API executor pair `PROJECT_CONFIGURED / 在项目配置时指定`. New and hydrated calibration nodes always serialize this fixed pair; actual project-level calibrator selection and runtime task assignment remain out of scope. Evidence: focused frontend Vitest 46/46, backend pytest 16/16, `vue-tsc --noEmit`, host and Docker production builds passed; frontend/backend containers were force-recreated, route `/performance/settings/templates/create` returned HTTP 200, the running frontend asset contains both the fixed code and label, and the backend container resolves the same pair; running frontend image `sha256:806e5ede68088ed214a00522214093e677dfe1d639961ac33846789fbe180e0f`, backend image `sha256:248713399eb675d48d627ad8d08b03d47fe9319306aa7798dedc0dcef440b002`; independent implemented-state PNG `../../ui-blueprints/PM-T004-T02-F33-implemented-calibration-executor.png`.

  - [x] PM-T004-T02-F34 Add shared multi-role executor configuration for result reconsideration; preserve legacy executor fields and defer runtime resolution to cycle snapshots. Evidence: frontend focused Vitest 46/46, backend workflow pytest 24/24, `vue-tsc --noEmit`, and frontend production build passed; independent implemented-state PNG `../../ui-blueprints/PM-T004-T02-F34-implemented-result-reconsideration-executors.png`. Runtime task resolution, organization snapshot extension, and specified-person search API remain explicitly out of scope.
    - Goal: configure result-reconsideration executors with HRBP default, employee_no-backed specified people, manager/department hierarchy rules, and shared frontend/backend normalization.
    - Type: frontend/API contract/backend normalization/test/documentation.
    - Prerequisites: PM-T004-T02-F15, PM-T004-T02-F26, PM-T004-T02-F30, PM-T004-T02-F33; confirmed HRBP default, employee_no identity, and organization snapshot semantics.
    - Allowed modifications: performance executor components/options, workflow settings view/spec, performance API types, template workflow router/service/tests, this feature directory, and one independent F34 PNG.
    - Forbidden modifications: cycle/project schema, cycle snapshots, runtime task generation/status, permissions, UCP, external systems, existing migrations, unrelated node behavior.
    - Shared files and merge rule: retain legacy `executor_types`/`executor_label`; add optional `executor_config`; old nodes normalize from legacy fields and existing components keep their public props.
    - Input contract: `executor_config.mode=MULTI_ROLE`; roles `REAL_LINE_MANAGER`, `HRBP`, `DEPARTMENT_HEAD`, `SPECIFIED_PERSON`; manager levels `DIRECT_MANAGER`, `LEVEL_1_MANAGER`; department levels `CURRENT_DEPARTMENT`, `PARENT_DEPARTMENT`, `LEVEL_1_DEPARTMENT`; people use `employee_no` plus `display_name`; default HRBP; at least one role; selected manager/department role requires a level; specified-person role requires a person.
    - Output contract: GET/PATCH round-trips canonical config in existing workflow JSONB; legacy fields remain unchanged for existing nodes; result-reconsideration `executor_label` is a compatibility summary and `executor_types` is a compatibility projection.
    - UI: existing workflow route; result-reconsideration panel renders the captured 319.333x519 geometry, shared checkbox/selector primitives, default HRBP, nested manager/department options, employee-name selector, validation, and locked states. No information-architecture change; independent implemented PNG required.
    - API/database/permission/external-system impact: existing template workflow GET/PATCH and JSONB only; no migration, new endpoint, permission, UCP, or external-system impact. Runtime executor resolution remains cycle-snapshot scope.
    - Test contract: Given default, valid multi-role, empty-role, missing-level, duplicate-person, unknown-role, legacy, and locked nodes; When hydrate, toggle, save, normalize, and reopen; Then default HRBP is present, invalid configs return `TEMPLATE_WORKFLOW_INVALID`, employee_no is retained while display_name is shown, legacy nodes remain unchanged, and existing executor regressions pass.
    - Acceptance: UI -> PATCH payload -> JSONB normalization -> GET reopen; old nodes render unchanged; required validation and locked behavior remain intact; no runtime enforcement claim.
    - Blockers: exact organization hierarchy source fields remain owned by cycle/organization snapshot service; this task must not invent a second hierarchy source.
    - Non-scope: runtime task generation, collaborative result editing, cycle-start snapshot extension, organization API/search implementation, and migration.
    - Definition of done: development, UI, API round-trip, tests, screenshot, and evidence complete; only then mark `[x]`.
  - [x] PM-T004-T02-F35 Add calibration reason enablement and required controls using shared switch and checkbox components, with template API persistence.
    - Goal: calibration nodes expose the captured default-on “填写调整原因” setting and optional “必填” control through reusable primitives.
    - Type: frontend/API contract/backend normalization/test/documentation.
    - Prerequisites: PM-T004-T02-F26 and PM-T004-T02-F33; supplied 2026-08-18 SnapSpec captures.
    - Required reading: feature `spec.md`, `ui-interaction.md`, performance workflow API/service/view, Spec 012 UI guardrails, and both supplied SnapSpec files.
    - Allowed modifications: performance checkbox/executor components and specs, workflow settings view/spec, performance API types, template workflow router/service/tests, this feature directory, and one independent F35 PNG.
    - Allowed new files: `PerformanceCheckbox.vue`, its focused spec, and `../../ui-blueprints/PM-T004-T02-F35-implemented-calibration-reason.png`.
    - Forbidden modifications: project/cycle schema, runtime calibration/task behavior, permissions, UCP, external systems, existing migrations, and F34 result-reconsideration behavior.
    - Shared files and merge rule: preserve unrelated dirty-worktree changes; retain existing switch public props and replace embedded manager-level checkbox markup with the shared checkbox without changing its emitted `executor_types` contract.
    - Input contract: `calibration_reason_enabled: boolean` defaults `true` for calibration; `calibration_reason_required: boolean` defaults `false` and may be true only while enabled.
    - Output contract: GET/PATCH returns both normalized flags; disabled and non-calibration nodes return `calibration_reason_required=false` and non-calibration nodes return both flags false.
    - UI: existing workflow route; reuse `PerformanceSwitchSettingRow` and `PerformanceSwitch`; share `PerformanceCheckbox` with manager-level options. Helper top gap `4px`; checkbox top gap `8px`; control `16×16px`; no new full-page blueprint because information architecture is unchanged; independent implemented PNG required.
    - API/database/permission/external-system impact: existing workflow DTO and JSONB only; no migration, new route, permission, UCP, or external-system impact.
    - Test contract: Given new, hydrated, enabled-required, disabled-required, non-calibration, and locked nodes; When toggle, check, save, normalize, and reopen; Then defaults, conditional visibility, clearing invariant, shared-component reuse, and JSONB fields are correct.
    - Acceptance: UI -> PATCH payload -> service normalization -> GET reopen; focused component/page/backend tests, typecheck, production/container build, route verification, and implemented-state PNG.
    - Evidence: performance component/workflow Vitest 75/75 passed; backend workflow pytest 23/23 passed with one existing Starlette deprecation warning; `vue-tsc --noEmit`, host production build, and Docker frontend/backend builds passed. The containers were force-recreated; `/performance/settings/templates/create` returned HTTP 200; running assets contain `calibration_reason_enabled`, `#1456f0`, and `width:271px`; backend container normalization proves disabled-required becomes false. Running frontend image `sha256:bfcd912253a68ead0c199b3b59b1bc80da03b159ddcf0cc910a9da49420a69d2`, backend image `sha256:065824601d2c16b9d0c2411b5ce675f643664a201437be9774239b231c36fbc4`; independent implemented-state PNG `../../ui-blueprints/PM-T004-T02-F35-implemented-calibration-reason.png`. Follow-up pixel correction on 2026-08-18 changed the required text into the checkbox's second sibling with an `8px` gap; focused Vitest passed 48/48, Docker frontend production build and forced recreation passed, the route returned HTTP 200, and running assets contain `performance-checkbox__control` plus `margin-left:8px`. Running frontend image: `sha256:0288afeaac52de8e2b3e95a1d2e285283da46c735b6db07b0226fc2113e98292`. The current full `vue-tsc --noEmit` is blocked by the unrelated pre-existing `PerformanceMultiExecutorField.spec.ts:11` `VueNode.checked` typing error.
    - Blockers: none for the confirmed template-only scope.
    - Non-scope: collecting adjustment text, runtime validation, project calibrator selection, task generation, cycle snapshots, and migration.
    - Definition of done: development, UI, API round-trip, tests, screenshot, and evidence complete; only then mark `[x]`.
  - [x] PM-T004-T02-F36 Add the result-reconsideration “发起复议提示” configuration and captured edit modal.
    - Required reading: feature `spec.md`, `ui-interaction.md`, supplied SnapSpec modal measurements, Spec 012 N000/N00/N11, workflow API types/router/service, and existing result-reconsideration executor implementation.
    - Allowed modifications: workflow settings view/spec, performance API type, template workflow router/service/tests, this feature directory, and the independent F36 blueprint/PNG.
    - Input/output contract: optional legacy `appeal_prompt_content` and `appeal_reason_instruction`; result-reconsideration nodes default both fields and persist at most 1,500/1,000 characters through existing GET/PATCH JSONB, while other node types normalize them to empty.
    - UI contract: `263.333×32px` edit entry; `600×352px` natural modal; two shared `552×49.333px` required textareas; vertical resize; internal live `/1500` and `/1000` suffixes; Save commits both local values, Cancel/Close/mask discard both drafts; prompt content remains editable for used templates.
    - API/database/permission/external-system impact: existing template workflow endpoint and JSONB only; no migration, new endpoint, permission, UCP, or external system.
    - Tests: focused workflow Vitest covers entry/default/cancel/save/geometry; backend pytest covers default, round trip, and 1,000-character validation; `vue-tsc --noEmit` and production build must pass.
    - UI compliance: Spec 012 N0001/N0002/N0004/N0005 and N1102-N1104 applied; no parallel design system or nested-card layout introduced.
    - Evidence: initial frontend focused Vitest 47/47 and backend workflow pytest 25/25 passed. Follow-up measured two-field correction: focused Vitest 48/48, backend workflow pytest 26/26 with one existing Starlette deprecation warning, `vue-tsc --noEmit`, and production build passed. Preview/spacing follow-up: focused Vitest 48/48, `vue-tsc --noEmit`, and production build passed; preview delay, immediate leave, content, executor-derived copy, clamped positioning, and the read-only `请输入复议理由` textarea are covered in `PerformanceTemplateWorkflowSettings.spec.ts`. V2 blueprint `../../ui-blueprints/PM-T004-T02-F36-appeal-prompt-v2.html`; independent V2 PNGs `../../ui-blueprints/PM-T004-T02-F36-implemented-appeal-prompt-v2.png` and `../../ui-blueprints/PM-T004-T02-F36-implemented-appeal-preview.png`.
    - Non-scope: employee-side appeal submission page/API, appeal records/actions, deadline/once-per-cycle enforcement, notifications, and runtime executor resolution.
    - Definition of done: development, UI, API round-trip, tests, screenshot, and evidence complete; only then mark `[x]`.
  - [x] PM-T004-T02-F37 Add the result-view fixed subject executor and persisted subject-confirmation switch.
    - Goal: `result_view` displays the shared required `SUBJECT / 被评估人` executor and a default-off `需要被评估人确认绩效结果` switch.
    - Type: frontend/API contract/backend normalization/test/documentation.
    - Prerequisites: PM-T004-T02-F15, PM-T004-T02-F30, and the existing `subject_confirm_required` contract in `spec.md`.
    - Required reading: feature `spec.md`, `ui-interaction.md`, workflow frontend/API/service/tests, supplied SnapSpec, and Spec 012 N000/N00/N11 UI checks.
    - Allowed modifications: shared performance executor/switch consumers, workflow view/spec, performance API type, workflow router/service/tests, and this feature directory.
    - Forbidden modifications: cycle/project schema, migrations, runtime result-confirmation tasks, permissions, UCP, external systems, and unrelated node behavior.
    - Input/output contract: `subject_confirm_required: boolean` defaults `false`; only `result_view` may retain `true`; result-view executor always normalizes to `executor_types=['SUBJECT']` and `executor_label='被评估人'`.
    - UI: existing workflow route; reuse `WorkflowFixedExecutorField`, `PerformanceExecutorTag`, `PerformanceSwitchSettingRow`, and `PerformanceSwitch`; no new page or information-architecture change.
    - API/database/permission/external-system impact: existing GET/PATCH workflow DTO and JSONB only; no migration, new endpoint, permission, UCP, or external-system impact.
    - Test contract: Given new, hydrated, enabled, disabled, non-result-view, and locked nodes; When render, toggle, normalize, save, and reopen; Then shared components render, default is off, true round-trips only for result view, other nodes clear the field, and fixed subject executor values are canonical.
    - Acceptance: UI -> PATCH payload -> service normalization -> GET reopen; focused frontend/backend tests, typecheck, container production builds, route verification, and running-asset verification.
    - Evidence: frontend workflow/component Vitest 51/51 passed; backend workflow pytest 26/26 passed with one existing Starlette deprecation warning; `vue-tsc --noEmit` passed; Docker frontend/backend production builds and forced container recreation passed. `/performance/settings/templates/create` returned HTTP 200; running frontend image `sha256:56a4265ecf50624b173419acfae2a565fced9073e1e51f55964cd8a57f88d6c7`, backend image `sha256:07f150c82464d01a0330f92445ead19dd430fa08931330af79593f08762ec94b`; production assets contain `subject_confirm_required` and `需要被评估人确认绩效结果`. Spec 012 UI checks N0001/N0002/N0004/N0005 and N1102-N1104 applied; no forbidden UCP or information-architecture change.
    - Blockers: none for template-only scope.
    - Non-scope: runtime confirmation task generation/completion, cycle snapshots, result publication behavior, migration, and permission changes.
    - Definition of done: development, UI, API round-trip, tests, build, runtime verification, and evidence complete; only then mark `[x]`.

- [ ] PM-T004-T05 内容设置整体架构与空态还原
  - Goal: 根据用户提供的 `screenshot-20260819-112335.png` 与 SnapSpec，实现内容设置三栏整体架构。
  - Type: frontend/UI
  - Prerequisites: PM-T004-T02；用户确认蓝图 `PM-T004-T05-content-settings.md`。
  - Required reading: `spec.md`、`ui-interaction.md`、`ui-implementation-guardrails.md`、PM-T004-T05 蓝图及 Spec 012 N0001/N0002/N0004/N0005/N1102-N1104。
  - Allowed modifications: `PerformanceTemplateCreate.vue`、`PerformanceTemplateContentSettings.vue`、对应 focused spec、PM-T004-T05 蓝图与实现截图。
  - Forbidden modifications: 后端 API/DTO、数据库迁移、权限模型、UCP、内容编辑器业务规则、流程设置组件。
  - UI: `/performance/settings/templates/create?step=content`；左侧流程列表、中间配置查看内容、右侧 `text-normal` 空态。
  - Test contract: Given content step and a saved template workflow; When rendered; Then every configured workflow node is listed in order with its API-provided name and executor, while icon geometry and typography follow the captured SnapSpec.
  - Acceptance: focused Vitest、`vue-tsc --noEmit`、生产构建；目标参考图已归档为 `../../ui-blueprints/PM-T004-T05-content-settings-reference.png`；实现截图 `../../ui-blueprints/PM-T004-T05-content-settings-implemented.png` 待浏览器截图工具可用后补齐。
  - Non-scope: 添加内容/提示/参考内容弹层，右侧节点编辑表单，真实内容保存接口。
  - Definition of done: development、UI、tests、build、screenshot and evidence complete; only then mark `[x]`.
