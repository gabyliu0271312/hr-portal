# AI + Excel 计算字段前端交互示意图

> **文档状态（2026-07-18）**：历史专项交互参考。当前全局 AI 助手和业务嵌入式结果卡是优先入口，三栏工作台仅作为远期深度分析模式；组织与人员调整助手复用 `AdjustmentDraftCard` 接入全局助手，不复制一套聊天框。

版本：v0.1  
日期：2026-06-08  
适用范围：AI 基础配置、Excel 公式组件、数据集级计算字段、报表编辑器接入

设计前提：

```text
新建计算字段是 AI 原生 HR 工作台下的一个场景能力。
前端不直接拼 prompt 调模型，而是调用统一 AI 编排层或已注册 Capability。
```

## 1. 总体入口流

```text
报表列表
  -> 新建/编辑报表
    -> 选择数据来源 = 数据集
      -> 报表设置 / 可选字段
        -> 新建字段
          -> 公式字段编辑抽屉
            -> 自然语言生成公式
            -> 校验/预览
            -> 保存为数据集计算字段
              -> 回到可选字段
                -> 计算字段分组出现新字段
```

注意：

- 单表报表首期不显示“新建字段”。
- 数据集报表才显示“新建字段”。
- 保存后的计算字段属于数据集，不属于单个报表。

## 2. 报表编辑器入口

位置：`ReportDesigner.vue -> ReportFieldWorkbench.vue`

```text
┌────────────────────────────────────────────────────────────────────┐
│ 报表设置（5 个字段）                                                │
├────────────────────────────────────────────────────────────────────┤
│ 出数规则                                                           │
│ [明细/聚合] [默认统计] [默认拆分] [余差收口]                         │
├────────────────────────────────────────────────────────────────────┤
│ 已选字段                                                           │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐                 │
│ │ 工号          │ │ 姓名          │ │ 基本工资      │                 │
│ └──────────────┘ └──────────────┘ └──────────────┘                 │
├────────────────────────────────────────────────────────────────────┤
│ 可选字段                                      [+ 新建字段]          │
│                                                                    │
│ 花名册 roster                                        18 个          │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐                 │
│ │ 部门          │ │ 职位          │ │ 入职日期      │                 │
│ └──────────────┘ └──────────────┘ └──────────────┘                 │
│                                                                    │
│ 工资 salary                                         12 个          │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐                 │
│ │ 基本工资      │ │ 目标奖金      │ │ 社保基数      │                 │
│ └──────────────┘ └──────────────┘ └──────────────┘                 │
│                                                                    │
│ 计算字段                                            2 个           │
│ ┌──────────────┐ ┌──────────────┐                                  │
│ │ 个税金额      │ │ 年包收入      │                                  │
│ └──────────────┘ └──────────────┘                                  │
└────────────────────────────────────────────────────────────────────┘
```

交互规则：

- 点击“新建字段”打开右侧抽屉。
- 计算字段卡片显示 `calc.xxx` 字段 code。
- 计算字段卡片可像普通字段一样加入已选字段。
- 计算字段若敏感，显示“敏感”标签。

## 3. 新建字段抽屉

组件建议：`FormulaFieldEditor.vue`

```text
┌──────────────────────────────────────────────────────────────┐
│ 新建计算字段                                           [×]   │
├──────────────────────────────────────────────────────────────┤
│ 基本信息                                                     │
│ 字段名称  [ 个税金额____________________ ]                    │
│ 字段编码  [ calc_tax_amount_____________ ]                    │
│ 数据类型  [ 数值 v ]       聚合角色 [ 度量 v ]                 │
│ 描述      [ 根据基本工资计算个税_________ ]                    │
├──────────────────────────────────────────────────────────────┤
│ AI 生成公式                                                  │
│ 自然语言描述                                                 │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 用基本工资计算个税，如果为空则返回0                       │ │
│ └──────────────────────────────────────────────────────────┘ │
│ [生成公式] [清空]                                            │
├──────────────────────────────────────────────────────────────┤
│ 公式编辑                                                     │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ =IF(ISBLANK(FIELD("salary.基本工资")),0,                 │ │
│ │     CALC_TAX(FIELD("salary.基本工资")))                  │ │
│ └──────────────────────────────────────────────────────────┘ │
│ [插入字段] [插入函数] [校验公式]                              │
├──────────────────────────────────────────────────────────────┤
│ 校验结果                                                     │
│ ✓ 公式合法                                                   │
│ ✓ 依赖字段：工资.基本工资                                    │
│ ⚠ 结果继承敏感属性，因为依赖“基本工资”                        │
├──────────────────────────────────────────────────────────────┤
│ 预览                                                         │
│ ┌──────────────┬──────────────┬──────────────┐              │
│ │ 基本工资      │ 个税金额      │ 状态          │              │
│ ├──────────────┼──────────────┼──────────────┤              │
│ │ 15000        │ 790          │ 正常          │              │
│ │              │ 0            │ 空值兜底      │              │
│ └──────────────┴──────────────┴──────────────┘              │
├──────────────────────────────────────────────────────────────┤
│                                      [取消] [保存并加入报表]  │
└──────────────────────────────────────────────────────────────┘
```

交互规则：

- “生成公式”调用 LLM 草稿接口。
- LLM 结果只回填草稿，不自动保存。
- “校验公式”调用后端校验接口。
- 只有校验通过才允许保存。
- “保存并加入报表”保存到数据集，并把字段加入当前报表已选字段。

## 4. LLM 生成草稿状态

```text
初始
┌──────────────────────────────┐
│ 输入自然语言描述              │
│ [生成公式]                    │
└──────────────────────────────┘

生成中
┌──────────────────────────────┐
│ 正在生成公式...               │
│ [按钮 loading]                │
└──────────────────────────────┘

生成成功
┌────────────────────────────────────────────────┐
│ AI 建议                                         │
│ 字段名：个税金额                                │
│ 公式：=CALC_TAX(FIELD("salary.基本工资"))        │
│ 说明：使用基本工资字段作为输入计算个税。          │
│ 公式已更新到上方编辑区。确认无误后保存字段。       │
└────────────────────────────────────────────────┘

生成失败
┌────────────────────────────────────────────────┐
│ 生成失败：模型暂不可用或输出不符合格式。          │
│ [重试] [手工编写公式]                            │
└────────────────────────────────────────────────┘
```

对话交互规则：

- AI 生成或调整公式成功后，公式编辑区直接展示最新公式，不再要求用户点击“应用到公式”。
- 用户不满意时，直接继续输入调整要求；系统根据当前公式和对话上下文继续生成。
- 不提供“继续调整”“重新生成”这类额外按钮作为主要路径；自然对话就是调整入口。
- 用户只是询问“某个函数怎么写”“当前月份公式是什么”时，只在对话区回答，不写入公式编辑区。
- 只有识别为生成/调整/修复公式意图时，才更新公式编辑区。

## 5. 插入字段弹窗

```text
┌──────────────────────────────────────────────┐
│ 插入字段                                      │
├──────────────────────────────────────────────┤
│ 搜索 [ 基本工资________________ ]             │
│                                              │
│ 花名册 roster                                 │
│  ○ 部门                roster.部门            │
│  ○ 入职日期            roster.入职日期        │
│                                              │
│ 工资 salary                                  │
│  ● 基本工资            salary.基本工资  敏感  │
│  ○ 目标奖金            salary.目标奖金  敏感  │
│                                              │
│                              [取消] [插入]    │
└──────────────────────────────────────────────┘
```

插入结果：

```text
FIELD("salary.基本工资")
```

前端可显示为：

```text
[工资.基本工资]
```

## 6. 插入函数弹窗

```text
┌──────────────────────────────────────────────┐
│ 插入函数                                      │
├──────────────────────────────────────────────┤
│ 搜索 [ tax____________________ ]              │
│                                              │
│ 系统函数                                      │
│  ○ IF(condition, true_value, false_value)     │
│  ○ ROUND(number, digits)                      │
│                                              │
│ 自定义函数                                    │
│  ● CALC_TAX(amount)                           │
│    根据输入金额计算个税                         │
│  ○ ROUND_MONEY(amount)                        │
│    金额保留两位小数                            │
│                                              │
│                              [取消] [插入]    │
└──────────────────────────────────────────────┘
```

交互规则：

- 只展示已启用函数。
- `data_action` 类型函数首期显示“暂不可用”或不展示。
- 插入函数时只插入函数壳，例如 `CALC_TAX()`。

## 7. 校验错误示意

```text
┌──────────────────────────────────────────────────────────────┐
│ 校验结果                                                     │
│ ✕ 公式不可保存                                               │
│                                                              │
│ 错误                                                         │
│ - 字段 salary.不存在字段 不存在                               │
│ - 函数 WEBSERVICE 不允许使用                                  │
│                                                              │
│ 建议                                                         │
│ - 请从“插入字段”中选择字段，避免手工输入错误 code              │
│ - 如需外部数据，请先通过数据接入模块接入，不允许公式访问网络   │
└──────────────────────────────────────────────────────────────┘
```

## 8. 保存后回到报表编辑器

```text
保存成功
  -> Toast: 已保存计算字段“个税金额”
  -> 关闭抽屉
  -> 刷新 allColumns
  -> 计算字段分组新增字段
  -> 若用户点击“保存并加入报表”，同步加入已选字段
```

示意：

```text
计算字段                                            3 个
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ 个税金额      │ │ 年包收入      │ │ 补偿基数      │
│ calc.tax_amt │ │ calc.annual  │ │ calc.comp   │
│ 敏感          │ │ 敏感          │ │ 敏感          │
└──────────────┘ └──────────────┘ └──────────────┘
```

## 9. 同数据集复用流程

```text
报表 A
  -> 数据集：月度薪酬数据集
  -> 新建计算字段：个税金额
  -> 保存

报表 B
  -> 数据集：月度薪酬数据集
  -> 打开报表编辑器
  -> 可选字段自动出现：计算字段 / 个税金额
```

不需要复制字段，也不需要在报表 B 重新创建。

## 10. AI 基础配置页面

建议位置：

```text
系统设置 -> 参数配置 -> AI 基础配置
```

定位：

```text
AI 基础配置只负责模型、Prompt 和 AI 策略。
不承载调用日志，不承载函数库，不承载 Capability 任意 URL 注册。
```

### 10.1 模型配置

```text
┌──────────────────────────────────────────────────────────────┐
│ AI 基础配置                                                   │
├──────────────────────────────────────────────────────────────┤
│ [模型配置] [Prompt 模板] [策略配置]                            │
├──────────────────────────────────────────────────────────────┤
│ 模型配置                                                     │
│ 能力标签        模型名称              状态       操作          │
│ fast_json       gpt-4o-mini           启用       编辑          │
│ reasoning       gpt-4.1               启用       编辑          │
│ low_cost        qwen-turbo            停用       编辑          │
│                                                              │
│ [新增模型配置]                                                │
└──────────────────────────────────────────────────────────────┘
```

### 10.2 Prompt 模板

```text
┌──────────────────────────────────────────────────────────────┐
│ Prompt 模板                                                   │
│ 模板编码              适用技能                    状态         │
│ formula_draft_v1      Excel 公式生成              启用         │
│ report_config_v1      报表配置生成                启用         │
│                                                              │
│ [编辑模板] [设为启用] [查看版本]                               │
└──────────────────────────────────────────────────────────────┘
```

### 10.3 策略配置

```text
┌──────────────────────────────────────────────────────────────┐
│ 策略配置                                                     │
├──────────────────────────────────────────────────────────────┤
│ 公式生成默认模型      [fast_json v]                            │
│ 失败重试次数          [2]                                      │
│ 单次输入最大长度      [2000]                                   │
│ 是否记录输入摘要      [开启]                                   │
│ 是否允许敏感字段入模  [关闭]                                   │
└──────────────────────────────────────────────────────────────┘
```

## 11. AI 能力管理页面

建议位置：

```text
系统设置 -> AI 管理 -> 能力管理
```

定位：

```text
AI 能力管理用于管理员维护 Capability 的启停、展示、示例问法、风险等级和评测入口。
不允许管理员在页面上注册任意 URL 或任意代码。
```

```text
┌──────────────────────────────────────────────────────────────┐
│ AI 能力管理                                      [运行评测]   │
├──────────────────────────────────────────────────────────────┤
│ 能力ID                       模块        类型      AI可见 状态 │
│ formula.generate             公式        draft     是     启用 │
│ formula.validate             公式        diagnose  是     启用 │
│ report.create_draft          报表        draft     是     启用 │
│ cost_allocation.run          成本分摊    write     否     启用 │
│                                                              │
│ 点击行查看：                                                   │
│ - 输入/输出 Schema                                             │
│ - 所需权限                                                     │
│ - 敏感策略                                                     │
│ - 确认策略                                                     │
│ - 示例问法                                                     │
│ - 最近失败                                                     │
│ - 评测用例                                                     │
└──────────────────────────────────────────────────────────────┘
```

交互规则：

- 管理员可以启停 `ai_visible`，控制模型是否可发现该能力。
- 管理员可以维护示例问法和风险等级。
- 管理员不能编辑工具实现，不能填写任意接口地址。
- 进入“评测用例”可以维护正常、失败、越权、敏感数据和低置信度回问案例。

## 12. 函数库管理页面

建议位置：

```text
系统设置 -> 参数配置 -> 函数库管理
```

定位：

```text
函数库管理是平台公共能力，不属于 AI 模块。
AI 公式助手、报表计算字段、补偿金工具、证明变量计算都可以引用函数库。
```

```text
┌──────────────────────────────────────────────────────────────┐
│ 函数库管理                                      [+ 新增函数]   │
├──────────────────────────────────────────────────────────────┤
│ 函数名        类型              返回值       状态      操作     │
│ CALC_TAX      system_builtin    number       启用      编辑     │
│ ROUND_MONEY   expression        number       启用      编辑     │
│ GET_SALARY    data_action       number       停用      查看     │
└──────────────────────────────────────────────────────────────┘
```

交互规则：

- 系统函数只能查看说明和启停，不能编辑实现。
- 表达式型函数可以维护参数、返回值和公式体。
- `data_action` 类型函数首期只能登记和查看，不允许在计算字段中执行。
- 函数库提供给公式组件的“插入函数”弹窗使用。

## 13. 系统日志管理页面

```text
┌──────────────────────────────────────────────────────────────┐
│ 日志管理                                                     │
├──────────────────────────────────────────────────────────────┤
│ [操作日志] [接口日志] [AI 调用日志] [数据权限日志]              │
├──────────────────────────────────────────────────────────────┤
│ AI 调用日志                                                   │
│ 时间          用户       技能             状态       Token      │
│ 10:21         张三       formula_draft    成功       892        │
│ 10:18         李四       formula_draft    失败       421        │
│                                                              │
│ 点击行查看：                                                   │
│ - 输入摘要                                                     │
│ - 输出摘要                                                     │
│ - 使用字段清单                                                 │
│ - 使用函数清单                                                 │
│ - 错误信息                                                     │
└──────────────────────────────────────────────────────────────┘
```

日志不展示完整敏感明细，只展示摘要、hash、字段清单和状态。

定位：

```text
AI 调用日志归入统一日志管理模块，不在 AI 基础配置中单独开日志页。
```

## 14. 权限与空状态

### 13.1 非数据集报表

```text
可选字段
[新建字段] 不显示
提示：计算字段首期仅支持数据集报表
```

### 13.2 无权限

```text
可选字段
[新建字段] 不显示
或按钮禁用并 tooltip：
“你没有新建计算字段权限”
```

### 13.3 无可用模型

```text
AI 生成公式区域
当前未配置可用模型，请联系管理员完成 AI 基础配置。
[手工编写公式]
```

## 15. 前端组件关系

```text
ReportDesigner.vue
  └─ ReportFieldWorkbench.vue
       ├─ available fields
       ├─ calculated fields group
       └─ FormulaFieldEditor.vue
            ├─ FormulaAiDrawer.vue
            ├─ FormulaFieldPicker.vue
            ├─ FormulaFunctionPicker.vue
            └─ FormulaPreviewPanel.vue

AiSettings.vue
  ├─ ModelConfigTab.vue
  ├─ PromptTemplateTab.vue
  └─ AiPolicyTab.vue

AiCapabilityManagement.vue
  ├─ CapabilityList.vue
  ├─ CapabilityPolicyPanel.vue
  └─ CapabilityEvalCases.vue

FunctionLibrary.vue
  └─ FormulaFunctionTab.vue

SystemLogs.vue
  └─ AiCallLogTab.vue
```

## 16. 关键确认点

- “新建字段”是数据集级能力，不是报表级能力。
- LLM 生成的是公式草稿，不自动保存。
- 公式保存前必须后端校验。
- 保存成功后，字段立即可加入当前报表。
- 其他报表只要基于同一数据集，就能复用该字段。
- 自定义函数首期不支持代码执行。
- 自定义函数归函数库管理，不放在 AI 基础配置。
- AI 调用日志归统一日志管理，不放在 AI 基础配置。
- 敏感依赖自动继承到计算字段。
- AI 能力管理只能维护 Capability 启停、展示、示例问法、策略和评测，不能注册任意 URL。
