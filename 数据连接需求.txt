你的困惑是正确的。当前“添加数据资源”的交互，把两类不同层级的东西混在了一起了，所以会让人感觉不合理。
先给结论
对于“飞书招聘”这类标准 SaaS：
不应该由用户逐个手动添加飞书招聘的原始 API 接口。
正确方式应是：
text
￼
￼
接入系统：飞书招聘
    ↓
配置一次飞书应用凭证 + 授权权限
    ↓
系统自动识别/加载飞书招聘已支持的业务能力
    ↓
用户在流水线中选择业务能力，而不是手工添加 API URL
例如用户应看到：
text
￼
￼
飞书招聘
├─ 应聘者
│  ├─ 查询应聘者
│  ├─ 获取应聘者详情
│  └─ 查询应聘进度
├─ Offer
│  ├─ 查询 Offer
│  └─ 获取 Offer 详情
├─ 职位
│  ├─ 查询职位
│  └─ 获取职位详情
└─ 面试
   ├─ 查询面试安排
   └─ 获取面试详情
而不是让用户看到：
text
￼
￼
/ hire / v1 / offers
/ hire / v1 / applications
/ hire / v1 / jobs
...
“资源”到底应该是什么
目前 UCP 的实现中，UcpResource 的定义是“一张表 / 一个 API”，并且一个资源绑定一个适配器和一份协议配置：backend/app/ucp/models.py:1271。
因此当前交互是：
text
￼
￼
飞书招聘系统
→ 手工添加资源
→ 选择适配器
→ 填写资源参数
这对以下场景是合理的：
• 北森某一个自定义报表；
• 飞书某一张在线表格或多维表格；
• 企业内部自建 API；
• 供应商给出的特殊接口；
• 每个客户 URL、路径、参数都不同的 HTTP API。
因为这些资源确实是企业自己定义的，需要管理员明确配置。
但对飞书招聘这种标准化开放平台，它不合理。飞书已经定义了 API、参数、返回结构和权限 Scope；用户不应该再手工“创建接口”。
你的理解中，哪些是对的
你说：
添加接口不需要做开发，只需要和飞书招聘打通后，添加好接口权限应该就可以获取到了。
这个判断方向正确，但需要补充一个边界：
• 管理员不需要逐个开发或录入接口：对；
• 只授予权限就能自动使用所有接口，平台完全不用开发：不完全对。
原因是平台仍需在第一次支持“飞书招聘”时，开发一次通用的飞书招聘连接器能力包，其中定义：
• 调用哪个飞书招聘 API；
• 每个 API 的输入参数；
• 分页与限流规则；
• 返回字段如何标准化；
• 哪些 Scope 是必需的；
• 如何把“应聘者”“Offer”“职位”等业务对象呈现给用户；
• 如何处理权限不足、字段缺失、接口版本变化。
但这属于平台开发一次。完成后，后续各业务人员只要：
1. 新建“飞书招聘”系统；
2. 填 App ID / App Secret；
3. 在飞书开放平台授予对应权限；
4. 点击“测试连接 / 同步能力”；
系统就应自动展示其有权限使用的能力，不需要再次创建每个接口。
更合理的目标交互
建议把“添加数据资源”改成按接入类型分流，而不是所有系统都进入同一套手工资源表单。
1. 标准 SaaS：飞书招聘、滴滴、北森等
text
￼
￼
添加系统：飞书招聘
→ 填凭证
→ 校验权限 Scope
→ 自动加载已支持业务能力
→ 按权限显示可用能力
页面中显示：
text
￼
￼
已连接：飞书招聘

可用能力
☑ 应聘者查询
☑ Offer 查询
☑ 职位查询
□ 面试安排查询（当前应用尚未授权）

[重新校验权限] [查看权限说明]
资源由系统自动创建或动态展示；用户只需要启用/停用某项能力、配置业务筛选条件或在流水线中引用。
2. 文件、表格、报表、自建 API：飞书在线表格、飞书多维表格、北森报表、通用 HTTP
这些仍使用当前“添加数据资源”的模式：
text
￼
￼
添加资源
→ 粘贴表格链接 / 选择报表 / 填 API 地址
→ 自动解析或填写少量参数
→ 测试连接
因为每张表、每份报表、每个自建 API 都是企业自己的实际对象，不能自动猜出应该接哪一个。
对现有系统的评估
当前系统已经有两个基础：
• 适配器注册表 + Schema 驱动表单：backend/app/ucp/models.py:1197、frontend/src/views/ucp/tabs/SystemsTabView.vue:638；
• 已有部分飞书招聘能力代码，例如 Offer 查询适配器：backend/app/ucp/adapters.py。
但现状仍是“资源需人工创建、适配器需人工选择”的通用模式，缺少以下关键能力：
1. 接入类型能力包
明确“飞书招聘”支持哪些标准业务对象和动作。
2. 权限 Scope 校验与能力发现
根据当前飞书应用的授权，判断哪些能力可用、哪些缺权限。
3. 自动资源初始化
飞书招聘连接成功后，自动生成或展示“应聘者 / Offer / 职位”等资源，而不是让用户手填资源编码。
4. 资源与动作的层级拆分
不能把每个原始 API URL 都当成用户可见资源。
应是：
text
￼
￼
业务对象：Offer
  ├─ 动作：查询列表
  ├─ 动作：查询详情
  └─ 动作：处理回调事件
而不是：
text
￼
￼
资源：/hire/v1/offers
资源：/hire/v1/offers/{id}
最终建议
后续应将 UCP 的资源分成两类：
类型
用户是否手工新增资源
典型例子
标准能力资源
否，连接后自动加载
飞书招聘、滴滴、北森标准 API
实例化数据资源
是，需要指定具体对象
飞书表格、多维表格、北森报表、自建 HTTP API
￼
所以，“添加数据资源”不该是飞书招聘接入后的必经步骤；它应只在需要配置企业具体对象时出现。对于飞书招聘，正确入口应是：连接成功 → 校验权限 → 自动启用可用业务能力 → 流水线直接选择能力/动作。
￼
￼
14:35
以批判性思维和系统实践进行强的系统性评估和规划，包括给出交互
14:38
￼
下面是基于当前系统现状、可扩展性、用户心智、运行治理与后续接入成本的强系统性评估与规划。
一、核心结论
当前 UCP 的“接入系统 → 添加资源 → 选择适配器”模型，不能作为所有接入系统的统一用户交互。
它把三类本应分开的概念混成了一层：
text
￼
￼
1. 外部系统连接：飞书招聘、北森、滴滴
2. 外部系统能力：应聘者、Offer、职位、面试
3. 企业具体实例：某张表、某份报表、某个自建 API
这会导致：
• 对飞书招聘、滴滴等标准 SaaS，用户被迫理解 API、资源编码、适配器；
• 对飞书表格、北森报表等实例型接入，当前手工配置反而是合理的；
• 平台后续每接一个 SaaS，都可能重复造“手工资源配置”的交互；
• 用户认为“我已经接入飞书招聘并授权了，为什么还要再创建资源”，这是完全合理的。
因此不建议简单优化“添加数据资源”弹窗，而应重构为：
统一底层模型，但按接入模式提供不同交互。
￼
二、应建立的分层模型
建议将 UCP 的连接体系拆为五层。
text
￼
￼
连接器能力包 Connector Package
        ↓
接入系统 Connection
        ↓
授权凭证 Credential
        ↓
业务能力 Capability
        ↓
企业实例 Resource Instance
        ↓
流水线 Pipeline
1. 连接器能力包
这是平台开发一次、持续维护的技术资产。
例如：
text
￼
￼
飞书招聘能力包
├─ 凭证类型：飞书 App ID / App Secret
├─ 认证方式：tenant_access_token
├─ 支持对象：应聘者、Offer、职位、面试
├─ 支持动作：查询列表、查询详情、事件订阅
├─ 所需权限：每项能力对应 Scope 要求
├─ API 映射：实际飞书 OpenAPI 路径、参数、分页、限流
└─ 输出模型：标准化字段与错误码
它不应由业务用户配置，也不应让用户手写 API URL。
当前 AdapterDefinition 更接近“能力包的一部分元数据”，而 UCP 的适配器代码、Schema、资源模型则分散在多个位置。后续应将它们收敛成“连接器能力包”这一明确概念。
2. 接入系统
接入系统代表一个外部厂商或业务系统实例，例如：
text
￼
￼
飞书招聘
北森 HR
企业滴滴
OA
Salesforce
系统层只管理：
• 名称、编码、业务域、负责人；
• 当前连接状态；
• 使用哪个连接器能力包；
• 可用凭证与环境；
• 能力启用状态。
它不应承担“具体 API 参数”或“某张表格定位”等实例配置。
3. 凭证
凭证是对外部系统的身份与授权，不是资源。
例如飞书招聘：
text
￼
￼
App ID
App Secret
租户信息
已配置 Scope 快照
凭证状态
最近验证时间
过期/失效状态
一个凭证可以被多个能力共同使用，例如：
text
￼
￼
飞书招聘凭证
├─ Offer 查询
├─ 应聘者查询
├─ 职位查询
└─ 面试查询
这正符合你的理解：先接通飞书招聘，配置权限，后续能力应自动可用，而不是每个 API 重新填凭证。
4. 业务能力
这是用户真正应该看见、选择和编排的对象。
飞书招聘的业务能力应是：
text
￼
￼
应聘者
├─ 查询应聘者列表
├─ 获取应聘者详情
└─ 应聘状态变更事件

Offer
├─ 查询 Offer 列表
├─ 获取 Offer 详情
└─ Offer 状态变更事件

职位
├─ 查询职位列表
└─ 获取职位详情
这里需要区分：
• 业务对象：Offer、职位、应聘者；
• 业务动作：查询列表、查询详情、订阅事件；
• 底层 API：只是能力实现细节，不作为普通用户的主交互对象。
5. 企业实例资源
“资源”应保留，但它只用于需要企业自己指定对象的场景。
例如：
text
￼
￼
飞书在线电子表格
└─ 某张具体花名册表

飞书多维表格
└─ 某个 App Token + Table ID + View ID

北森报表
└─ 某个 Report ID

通用 HTTP
└─ 某个企业内部 API 地址

数据库
└─ 某个数据源 + 查询语句
因此，资源不是要取消，而是要明确它的适用范围：
接入类别
是否手工新增资源
用户要配置什么
飞书招聘、滴滴、北森标准 API
通常否
凭证、授权、启用能力
飞书在线表格 / 多维表格
是
链接、表格、视图、读取范围
北森自定义报表
是
Report ID、报表参数
通用 HTTP / 自建 API
是
URL、方法、认证、请求参数
数据库
是
连接、表/SQL、增量字段
￼
￼
三、对当前交互的批判性评估
当前交互的问题
现在的路径大致是：
text
￼
￼
添加系统
→ 填系统名称和编码
→ 添加凭证
→ 添加资源
→ 选择适配器
→ 填写 Schema
这个流程默认假设：
每个接入系统都需要管理员先理解“资源”和“适配器”，并手工建立资源。
问题有五个。
1. 用户心智错误
业务用户的认知是：
text
￼
￼
我接飞书招聘，是为了使用 Offer、候选人、职位数据。
而不是：
text
￼
￼
我要创建一个 adapter_code = XXX 的 resource_code = XXX。
资源编码、适配器编码、协议字段属于平台实现细节。
2. 标准 SaaS 的价值被削弱
飞书招聘的价值是“标准 API 已经存在”。
如果用户仍要一个个录入资源，平台只是把 API 管理界面换了名字，没有真正降低接入成本。
3. 资源粒度不稳定
“一个接口一个资源”会迅速膨胀：
text
￼
￼
Offer 列表
Offer 详情
Offer 状态
Offer 回调
Offer 附件
...
这些其实多数属于同一业务对象或能力包，不应全部成为一级资源。
4. 容易出现重复配置
多个资源可能重复填写：
• 相同凭证；
• 相同 Base URL；
• 相同认证方式；
• 相同租户信息；
• 相同权限配置。
这会造成凭证轮换、权限变更、故障排查困难。
5. 不能形成能力治理
如果资源只是原始 API，监控只能告诉你：
text
￼
￼
/hire/v1/offers 请求失败
而业务方真正关心的是：
text
￼
￼
Offer 同步能力不可用
新员工入职流程可能延迟
治理对象必须向“业务能力”上收敛。
￼
四、推荐的信息架构与交互
建议将“添加数据资源”从系统通用必经步骤，改为按接入模式自动分流。
交互一：新建接入系统
text
￼
￼
第一步：选择接入方式

[标准系统接入]
飞书招聘 / 北森 HR / 企业滴滴 / OA / Salesforce

[数据对象接入]
飞书在线表格 / 飞书多维表格 / 北森报表 / 通用 API / 数据库
这一步非常关键。它决定后续流程，而不是让所有系统走同一条资源配置路。
￼
交互二：标准系统接入——以飞书招聘为例
text
￼
￼
添加系统：飞书招聘
        ↓
填写系统名称、负责人、环境
        ↓
填写飞书应用凭证
        ↓
[测试并识别能力]
        ↓
展示授权结果
页面应展示：
text
￼
￼
飞书招聘连接状态：已连接

凭证校验：成功
应用权限状态：部分可用

可用业务能力
✓ 应聘者查询
✓ Offer 查询
✓ 职位查询
○ 面试查询：未启用
× 面试评价查询：缺少飞书应用权限

[重新校验权限] [查看授权指引]
然后用户点击：
text
￼
￼
[启用 Offer] [启用 应聘者] [启用 职位]
系统自动创建“能力实例”或仅记录启用状态，不要求填写 API URL、资源编码、Adapter Code。
流水线画布中的选择方式
text
￼
￼
添加节点
→ 飞书招聘
→ Offer
→ 查询 Offer 列表
而不是：
text
￼
￼
添加节点
→ 选择资源代码
→ 选择 FEISHU_OFFER_DETAIL_ADAPTER
→ 手工填接口参数
￼
交互三：实例型数据资源——以飞书在线表格为例
text
￼
￼
添加系统：飞书在线表格
        ↓
填写飞书凭证
        ↓
粘贴飞书表格链接
        ↓
系统自动解析文件、工作表、范围
        ↓
保存资源
这类场景继续保留“资源”，因为用户确实需要表达：
text
￼
￼
我要读取的是哪一张具体表。
并且应采用你刚刚确认的规则：
text
￼
￼
链接识别成功
→ 自动解析并锁定 Token / Sheet ID
→ 不显示人工技术字段

链接无法识别
→ 只显示缺失的最小补充字段
￼
五、权限识别不能被过度承诺
你的判断“添加接口权限后应该就可以获取”是产品方向正确，但实施上要避免一个误区：
飞书应用拥有某个 Scope，不等于平台已经知道如何使用、展示和治理这个 API。
平台需要为每项能力维护最小元数据：
text
￼
￼
能力：Offer 查询
├─ 所需 Scope 列表
├─ API 路径
├─ 请求参数 Schema
├─ 返回字段 Schema
├─ 分页方式
├─ 限流规则
├─ 错误码映射
└─ 数据敏感等级
因此建议能力状态采用三段式，而不是简单“有权限 / 没权限”：
状态
含义
已支持且已授权
平台有能力实现，当前凭证可调用
已支持但未授权
平台知道如何调用，但当前飞书应用缺 Scope
未内置支持
飞书可能有 API，但当前平台尚未实现对应能力包
￼
这样用户不会误以为“飞书存在的所有接口，平台都自动支持”。
￼
六、建议的数据模型调整
不建议立即推翻现有 UcpSystem、UcpResource、UcpCredential，而是增量扩展。
text
￼
￼
UcpSystem
├─ connector_package_code
├─ connection_mode
│  ├─ capability_based
│  └─ instance_based
└─ credential_id

UcpCapabilityDefinition（平台内置定义）
├─ connector_package_code
├─ capability_code
├─ capability_name
├─ object_type
├─ action_type
├─ required_scopes
├─ adapter_code
├─ input_schema
├─ output_schema
└─ sensitivity

UcpSystemCapability（某系统已启用能力）
├─ system_id
├─ capability_code
├─ credential_id
├─ enabled
├─ authorization_status
├─ authorization_detail
├─ last_checked_at
└─ runtime_config

UcpResource（仅用于实例型对象）
├─ system_id
├─ resource_type
├─ locator_config
├─ adapter_code
└─ ...
建议保留 UcpResource，但不要继续让它承担标准 SaaS 的所有 API 资源建模。
￼
七、实施规划
Phase A：先纠正产品模型与交互
目标：不再让飞书招聘走“强制手工添加资源”。
1. 增加接入方式分类：
◦ 标准能力型；
◦ 实例资源型；
◦ 自定义 API 型。
2. 接入系统创建后自动分流：
◦ 飞书招聘进入“能力校验”；
◦ 飞书表格进入“添加具体资源”。
3. 调整文案：
◦ 标准系统页面不展示“添加数据资源”作为主要动作；
◦ 改为“校验并启用业务能力”。
4. 流水线节点改为：
◦ 系统 → 业务对象 → 动作；
◦ 资源仍用于表格/报表/自定义 API。
Phase B：建立飞书招聘能力包
目标：开发一次，后续用户不再创建单个飞书 API。
优先能力建议：
text
￼
￼
P0
- Offer 查询 / Offer 详情
- 应聘者查询 / 应聘者详情
- 职位查询

P1
- 面试查询
- Offer 状态事件
- 应聘状态事件

P2
- 附件、评价、复杂筛选
- 批量同步模板
每项能力必须包含：
• Scope 要求；
• 可测试调用；
• 错误提示；
• 脱敏规则；
• 运行审计；
• 流水线输入输出 Schema。
Phase C：能力发现与治理
目标：连接后用户只看到自己可用的能力。
1. 保存应用授权 Scope 配置快照；
2. 通过安全探测接口校验关键能力；
3. 生成能力矩阵；
4. 缺权限时给出业务化提示；
5. 权限变化后可重新校验；
6. 监控按“Offer 查询能力”而非原始 URL 告警。
Phase D：兼容现有资源模型
现有已创建资源不能破坏：
• 已有 UcpResource、流水线、执行记录继续运行；
• 飞书多维表格仍属于实例资源；
• 旧资源在页面中标注“自定义/实例资源”；
• 新的标准 SaaS 系统默认进入能力模式；
• 后续提供“将旧资源关联到标准能力”的迁移工具，但不自动迁移生产数据。
￼
八、最终推荐的页面结构
text
￼
￼
数据连接
├─ 接入系统
│  ├─ 飞书招聘
│  │  ├─ 概览
│  │  ├─ 凭证与授权
│  │  ├─ 业务能力
│  │  ├─ 事件订阅
│  │  ├─ 运行监控
│  │  └─ 变更记录
│  │
│  └─ 飞书在线表格
│     ├─ 概览
│     ├─ 凭证
│     ├─ 表格资源
│     ├─ 同步配置
│     └─ 运行监控
│
├─ 流水线
│  ├─ 从业务能力创建
│  └─ 从实例资源创建
│
└─ 连接器管理
   ├─ 标准能力包
   ├─ 自定义 API 模板
   └─ 权限 / Scope 映射
最终原则：
标准 SaaS 让用户“连接并启用能力”；
表格、报表、自建 API 让用户“添加具体资源”；
API、Token、Adapter Code 是平台内部实现，不应成为普通用户主界面。
￼
￼
14:38
飞书应用拥有某个 Scope，不等于平台已经知道如何使用、展示和治理这个 API。
对于这个举例，可以实现用户自动在前台添加和配置以及测试是否OK嘛，这样接入某个标准系统后，智能卡能力时不需要再次开发了，能够做到嘛
14:42
￼
已处理 32s
￼
可以做到，但要把目标说准确：
可以做到“接入标准系统后，用户在前台选择/配置/测试新的 API 能力，不再为每一个接口写后端代码”。
但不能承诺“只要有 Scope，系统就自动懂得所有 API 的业务含义、数据结构、风险和编排用途”。
我先把你说的“智能卡能力”理解为“智能识别并生成能力卡”；如果你指的是别的概念，请纠正我。
飞书的权限本身是按具体 API/事件能力授予的；它解决“能不能调”，不自动解决“平台应该把它叫什么、如何传参、如何分页、返回数据如何治理”。(
￼
feishu.apifox.cn)
￼
一、正确目标：从“资源配置”升级为“能力卡配置”
未来用户不该添加：
text
￼
￼
资源：/hire/v1/offers
适配器：FEISHU_XXX_ADAPTER
而应看到：
text
￼
￼
能力卡：查询 Offer
能力卡：查询应聘者
能力卡：查询职位
能力卡：查询面试安排
每张能力卡本质上是一个可配置、可测试、可治理的 API 动作定义。
text
￼
￼
飞书招聘系统
├─ 凭证：飞书应用
├─ 已授权能力
│  ├─ 查询 Offer
│  ├─ 查询应聘者
│  └─ 查询职位
├─ 待补充配置能力
│  └─ 查询某招聘项目下的 Offer
└─ 不可用能力
   └─ 查询面试评价（缺少权限）
用户选中“查询 Offer”后，只填写业务参数，例如：
text
￼
￼
筛选条件：招聘项目
分页大小：100
返回字段：候选人、Offer 状态、职位、创建时间
不填写 URL、HTTP Method、认证方式、Token、Adapter Code。
￼
二、能否做到“后续接口不再开发”
可以做到的部分：协议级零开发
平台一次性建设好“通用 API 能力引擎”后，后续新接口可由用户前台配置：
• 导入 OpenAPI / Swagger 文档；
• 选择某个 API；
• 自动生成请求参数表单；
• 自动生成响应预览；
• 自动继承飞书凭证认证；
• 自动执行测试；
• 自动保存为能力卡；
• 自动供流水线选择。
这类能力不再需要“每一个接口写一个 Python Adapter”。
例如用户在飞书招聘能力目录中选择：
text
￼
￼
GET /xxx/xxx
系统根据接口元数据自动生成：
text
￼
￼
能力名称：查询某某列表
输入参数：page_size、page_token、job_id
输出字段：items、has_more、page_token
认证：当前飞书应用凭证
所需权限：xxx:readonly
然后用户点击：
text
￼
￼
[测试能力] → [保存为能力卡] → [在流水线中使用]
这部分是可行且值得建设的。
不能完全自动的部分：业务级零开发
平台无法只凭 URL 和 Scope 自动知道：
text
￼
￼
这个接口在业务上到底是“Offer 查询”、
“员工入职状态”、
“需要审批的写操作”、
还是“包含高敏感个人信息的数据导出”。
也无法可靠自动判断：
• 哪个字段是员工唯一标识；
• 哪个字段适合作为增量时间；
• 哪个分页规则是否安全；
• 返回的附件、手机号、身份证是否必须脱敏；
• 写操作是否需要审批和幂等控制；
• 该 API 失败会不会影响入职、离职、账号回收等高风险流程。
所以必须将能力分级，而不是追求“完全无开发”。
￼
三、推荐的三层能力模式
模式
适用场景
是否需要后端新增代码
用户操作
标准能力卡
Offer、应聘者、职位、滴滴账号等高频业务能力
平台首次建设一次
接入后直接启用、测试、编排
可配置能力卡
飞书已公开、但平台未预置的只读 API
不需要逐接口开发
从 API 目录选择、补参数、测试、保存
受控自定义 API
企业内部 API、特殊供应商接口、写操作
不一定需要开发，但需要审核
填 URL/Schema、测试、提交审批后发布
￼
这才是可长期扩展的模式。
￼
四、建议的用户交互
1. 接入系统后，不直接进入“添加资源”
以飞书招聘为例：
text
￼
￼
添加接入系统：飞书招聘
→ 填写 App ID / App Secret
→ 测试连接
→ 识别可用能力
测试后进入“能力中心”：
text
￼
￼
飞书招聘 · 能力中心

连接状态：已连接
权限状态：12 项可用 / 3 项待授权

[已启用]
✓ 查询 Offer
✓ 查询应聘者
✓ 查询职位

[可添加]
+ 查询面试安排
+ 查询招聘项目
+ 查询 Offer 附件

[不可用]
× 查询面试评价
  缺少权限：xxx:readonly
  [查看授权说明]
用户看到的是业务能力，而不是资源与接口。
2. 用户添加一个平台未预置的接口
点击：
text
￼
￼
[添加能力]
进入：
text
￼
￼
方式一：从飞书招聘 API 目录选择
方式二：导入 OpenAPI 文档
方式三：添加受控自定义 API
选择 API 后，系统生成能力卡：
text
￼
￼
能力名称：[查询 Offer 附件                  ]
所属对象：[Offer                           ]
动作类型：[查询列表 / 查询详情 / 写入 / 事件]
认证方式：[使用当前飞书招聘凭证，不可修改   ]

输入参数
- offer_id              [必填]
- page_size             [默认 100]
- page_token            [自动分页]

返回处理
- 数据列表路径          [data.items]
- 下一页 Token 路径     [data.page_token]
- 是否自动分页          [开启]

[测试能力] [保存草稿] [提交发布]
3. 测试能力
测试必须分层：
text
￼
￼
第一层：凭证测试
- 能否获取 Token

第二层：权限测试
- 是否允许调用该 API

第三层：参数测试
- 输入参数是否正确

第四层：响应测试
- 返回结构是否可识别
- 是否检测到敏感字段
- 是否需要字段映射
测试成功后显示：
text
￼
￼
✓ 鉴权成功
✓ 权限满足
✓ 返回 20 条样例数据
! 检测到手机号、邮箱字段，默认按敏感字段处理

[保存为能力卡]
写操作则不能直接自动执行真实测试，应默认进入：
text
￼
￼
[模拟参数校验]
[请求预览]
[提交审批后测试]
￼
五、核心技术设计
要做到这件事，需要新增“通用 API 能力引擎”，而不是继续堆更多手工 Adapter。
text
￼
￼
Connector Package
├─ 厂商认证策略
├─ API 目录 / OpenAPI 来源
├─ Host 白名单
├─ 权限 Scope 映射
└─ 默认安全策略

Capability Definition
├─ 能力名称、业务对象、动作类型
├─ HTTP Method / Path
├─ 输入 Schema
├─ 响应 Schema
├─ 分页规则
├─ 数据提取路径
├─ 所需 Scope
├─ 读写风险等级
└─ 数据敏感等级

System Capability
├─ 所属接入系统
├─ 关联凭证
├─ 启用状态
├─ 参数覆盖配置
├─ 权限校验结果
├─ 最近测试结果
└─ 发布版本

Capability Run
├─ 请求摘要
├─ 响应摘要
├─ Trace ID
├─ 脱敏快照
├─ 成功/失败
└─ 审计记录
当前系统已有一部分基础：
• AdapterDefinition：已有 Schema 元数据能力；
• UcpResource：已有资源、凭证、调度和执行模型；
• Pipeline：已有编排与执行审计；
• UCP 适配器：已有代码执行入口。
但当前限制是：执行依然主要依赖后端 get_adapter() 中预先写好的代码适配器。
后续需要新增：
text
￼
￼
GenericHttpActionAdapter
它根据能力卡定义动态执行：
text
￼
￼
认证注入
→ 参数校验
→ URL / Query / Body 组装
→ SSRF 与 Host 白名单校验
→ API 请求
→ 分页处理
→ JSONPath / 字段提取
→ 脱敏
→ 标准化执行结果
这样新 API 不再需要增加一个新的 Python 函数。
￼
六、关键前提：API 目录从哪里来
这是方案能否真正“少开发”的关键。
建议支持三种来源：
A. 平台内置厂商 API 目录
适用于飞书招聘、滴滴、北森等高频系统。
平台维护一次：
text
￼
￼
飞书招聘 API 目录
├─ 名称
├─ 路径
├─ 参数定义
├─ Scope
├─ 版本
└─ 风险标签
优点：最安全、最友好、业务语义清楚。
缺点：平台首次要维护目录。
B. 导入 OpenAPI / Swagger
适用于供应商提供标准 OpenAPI 文档的系统。
用户上传或填写 OpenAPI 地址后：
text
￼
￼
解析 OpenAPI
→ 列出接口
→ 选择接口
→ 生成能力卡草稿
→ 测试